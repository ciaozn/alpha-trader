# Tasks 001: Alpha Trader 细粒度任务清单（P0 + P1 + P2 + P3）

| 字段 | 值 |
|---|---|
| 状态 | **P0+P1+P2 已完成**；**P3 进度 16/23**：T301-T314 完成，T315 进行中，T316-T320 待办，T321 阻塞 |
| 创建日期 | 2026-09-09 |
| 最近审计 | 2026-09-12：`mvn clean verify` 全绿，**576 个用例 0 失败**；P3 出口（场景 2/3）尚未通电验收 |
| 上游文档 | [plan.md](./plan.md)（Approved）· [spec.md](./spec.md)（Approved） |
| 生成策略 | 滚动式：本文件含 P0+P1+P2+P3 细任务；P4 及以后临近开工再细化 |

> 每条任务 ≤ 2 小时工作量，含验收命令。完成后勾选并记录验收结果。

---

## P0 工程骨架

| # | 任务 | 验收 | 状态 |
|---|---|---|---|
| T001 | 父 POM：groupId `com.ciaozn.alphatrader`，Java 21，dependencyManagement 锁定 Spring Boot 3.x / OkHttp / Java-WebSocket / Jackson / JUnit5 版本 | `mvn -q validate` 通过 | ☑ |
| T002 | alpha-common 模块：POM（零外部依赖，仅 jackson-annotations）+ 包结构 | 模块可独立编译 | ☑ |
| T003 | alpha-engine 模块：POM 仅依赖 common | 红线：无交易所/网络库依赖 | ☑ |
| T004 | alpha-gateway / strategy / risk / execution 模块：POM 依赖 engine | 依赖方向正确 | ☑ |
| T005 | alpha-backtest 模块：POM 依赖 strategy/risk/execution/gateway | 依赖方向正确 | ☑ |
| T006 | alpha-app 模块：Spring Boot 主类 + 依赖全部模块 | 应用可启动 | ☑ |
| T007 | application.yml 三 profile：backtest / paper / live，模式装配占位 | 三 profile 均可启动 | ☑ |
| T008 | 密钥注入：`.env.example` + `.gitignore` + 启动时 EnvValidator（缺失即 fail-fast） | 仓库 grep 零密钥 | ☑ |
| T009 | Logback：应用日志与事件日志分离，按天滚动 | 启动产生两类日志文件 | ☑ |
| T010 | GitHub Actions CI：编译 + 单测 | workflow 文件就绪 | ☑ |

**P0 出口**：`mvn clean verify` 全绿；`--spring.profiles.active=backtest|paper|live` 均可启动。

## P1 事件引擎 + 行情接入

| # | 任务 | 对应 spec | 验收 | 状态 |
|---|---|---|---|---|
| T101 | Event sealed interface + 全局序号分配器（AtomicLong） | EN-02 | 编译期穷举单测 | ☑ |
| T102 | 行情模型：Symbol（`BTCUSDT.PERP` 统一表示）/ Interval / Kline + KlineEvent / TickerEvent | GW-04 | 模型单测 | ☑ |
| T103 | 交易事件：SignalEvent / OrderRequest / OrderUpdateEvent / FillEvent / TimerEvent / RiskAlertEvent | EN-01 | 模型单测 | ☑ |
| T104 | Clock 抽象：SystemClock / VirtualClock（可推进、可定点触发定时器） | EN-03 | 单测 + grep 无业务侧 `System.currentTimeMillis()` | ☑ |
| T105 | EventEngine：单线程循环 + FIFO 队列 + 定时器优先队列 + handler 链（连锁事件单轮闭环） | EN-01/04 | 顺序性 + 连锁闭环 + 定时器触发单测 | ☑ |
| T106 | EventJournal：append-only JSONL 落盘 + 读取 | EN-02 | round-trip 单测 | ☑ |
| T107 | ExchangeGateway 接口 + GatewayConfig / OrderAck / AccountSnapshot / Position DTO | GW 前置 | 接口编译 | ☑ |
| T108 | TimeSync：启动校准交易所时间偏移，超阈值（默认 1000ms）告警 | 边界 8 | 偏移计算单测 | ☑ |
| T109 | BinanceFuturesGateway（行情部分）：Java-WebSocket 订阅 K 线 → 标准化 KlineEvent，testnet/实盘地址可配 | GW-01/05 | 本地 mock WS 单测 | ☑ |
| T110 | 指数退避重连（1s→60s 上限）+ 重连后重订阅 | GW-02 | 退避序列单测 | ☑ |
| T111 | EchoStrategy 占位 + app 装配：paper profile 打印 K 线事件 | P1 出口 | 应用启动可见事件打印 | ☑ |
| T112 | testnet 真实联调：60s 断网恢复验证 | P1 出口 | ⚠️ 需用户网络环境，标记为用户验收项 | ⊘ 跳过 |

**P1 出口**：T101-T111 全部通过且 `mvn clean verify` 全绿；T112 由用户在可连 testnet 的环境执行。

---

## P2 策略 + 回测

> 本阶段是"同构设计"的第一个兑现点：回测链路完全脱离网络运行，且策略/风控/OMS 代码与实盘共用（FR-BT-06）。
> 环境事实（2026-09-09 实测）：本机 **无法直连 `fapi.binance.com`（实盘 REST）与 `repo.maven.apache.org`**，
> 但 **可直连 `testnet.binancefuture.com`（REST+WS，含 2019 年至今 BTCUSDT 1h 历史）**。
> 因此 T211 下载工具以 testnet 为验收环境，base url 可配置（FR-GW-05）；Maven 走 `~/.m2/settings.xml` 镜像到 `repo1.maven.org`。

| # | 任务 | 对应 spec | 验收 | 状态 |
|---|---|---|---|---|
| T201 | `TradingRules`（tickSize/stepSize/minNotional）落 alpha-common，作为 Symbol 精度模型 | GW-03 前置、RK-07 | 取整/对齐工具单测 | ☑ |
| T202 | `Portfolio` + `Position` 领域模型（common）：成交应用（加仓均价/减仓实现盈亏/翻仓）、手续费、资金费、标记价与权益 | EX-06 前置、ST-01 | 加/减/翻仓 + 权益单测 | ☑ |
| T203 | Strategy SPI：`Strategy`（id/symbols/onKline/onFill/onTimer）+ `StrategyContext`（K 线窗口、持仓视图、Clock、emit 信号） | ST-01/05 | 接口编译 + emit 进 cascade 单测 | ☑ |
| T204 | `BarSeries` 定长环形缓冲：只收 closed bar，按 openTime 去重与丢弃乱序 | 边界 1 | 重复/乱序/容量单测 | ☑ |
| T205 | 指标库：SMA / EMA / RSI / ATR（流式纯计算，回测实盘同一实现） | ST-04 | 与 Python 参考实现逐值对比（EMA 以 SMA 播种、RSI/ATR 用 Wilder 平滑） | ☑ |
| T206 | `StrategyEngine`（EventHandler）：按 symbol 分发 closed K 线、喂 BarSeries、信号经 cascade 发出 | ST-01 | 分发 + 过滤非订阅 symbol 单测 | ☑ |
| T207 | `MaCrossStrategy`：金叉做多、死叉平多/翻空，fast/slow/allowShort 可配 | ST-02 | 构造 K 线序列触发预期信号单测 | ☑ |
| T208 | `RsiReversalStrategy`：超卖做多、超买平多，period/os/ob（+可选 ATR 波动过滤）可配 | ST-03 | 同上 | ☑ |
| T209 | `PositionSizer` + 风控直通闸门（alpha-risk）：信号 → 目标仓位增量 → stepSize 取整 → OrderRequestEvent；小于最小数量/名义则放弃并告警 | RK-07、边界 4 | 换算 + 取整 + 放弃告警单测 | ☑ |
| T210 | `KlineRepository` 契约（common）+ `CsvKlineRepository`（backtest，DESIGN §10 的 CSV 回放源） | BT-01 | CSV round-trip 单测 | ☑ |
| T211 | `JdbcKlineRepository`（alpha-app，SQLite/MySQL 同 schema）+ 建表 | BT-05、OP-04 前置 | 临时 SQLite 文件 round-trip 单测 | ☑ |
| T212 | `BinanceKlineDownloader`（gateway REST，1500 根分页、base url testnet/实盘可配、限速退避） | BT-05、GW-05 | 解析单测（mock JSON）+ testnet 真实拉取入库 | ☑ |
| T213 | `EventEngine.awaitQuiescence(eventId)`：回放方等本轮连锁闭环后再推进虚拟时钟 | BT-01、NFR-04 | 单测（等待返回时 cascade 已完成） | ☑ |
| T214 | `BacktestDataFeeder`：按时间序回放 + 推进 VirtualClock + 数据缺口检测记录 | BT-01、边界 3 | 顺序性 + 缺根记录单测 | ☑ |
| T215 | `SimulatedExecutor`：次根开盘价成交 + 滑点（固定 bp + 振幅比例）+ taker 手续费 + 资金费（8h 计提） | BT-02/03、边界 5 | 撮合规则单测 + **反未来函数断言**（信号根不成交、成交根不用未来价） | ☑ |
| T216 | `TradeTracker` + `PerformanceMetrics` + `PerformanceAnalyzer`：净值曲线、年化、夏普、最大回撤、胜率、盈亏比 | BT-04 | 手工构造净值/成交序列的公式单测 | ☑ |
| T217 | 报告输出：自包含 HTML（内联 SVG 净值曲线 + 逐笔明细 + 缺口）+ 控制台摘要 | BT-04 | 生成报告含全部指标，无外部 CDN 依赖 | ☑ |
| T218 | `BacktestRunner`：纯 Java 装配全链路（引擎/虚拟时钟/feeder/撮合/策略/风控直通/报告），不依赖 Spring | BT-06 | 固定数据集端到端跑通并出报告 | ☑ |
| T219 | alpha-app backtest 装配：`alpha.backtest.*` 配置（数据源 csv/db、策略启停与参数、初始资金、滑点费率）+ 跑完自动退出 | OP-01、ST-01 | `--spring.profiles.active=backtest` 出报告后进程正常退出 | ☑ |
| T220 | CI 回测冒烟：固定数据集 + 绩效指标阈值断言 + 同输入 3 次运行逐位一致 | SC-02、plan §5 | `mvn clean verify` 全绿（含冒烟测试） | ☑ |
| T221 | SC-01 性能验收：BTCUSDT 1h 近三年（≈26k 根）完整回测 + 报告 < 5 分钟 | SC-01 | 实测计时记录在执行日志 | ☑ |
| T222 | 策略工厂注册机制：`StrategyFactory` SPI + yml 按 id 启停传参，新增策略零改引擎 | ST-01、场景 6 | 新增一个 dummy 策略仅靠配置生效的单测 | ☑ |
| T223 | **（补）** 历史数据下载入口：`download` profile + `AlphaProperties.Download` + 抽出 `DataPlan`（「数据落在哪」这条规则由下载与回测共用） | BT-05、OP-01、SC-01 前置 | `--spring.profiles.active=download` 把区间内 K 线写进 `alpha.backtest.data`、打印 fetched/stored 后进程正常退出 | ☑ |

**P2 出口**：T201-T223 全部通过；`mvn clean verify` 全绿；backtest profile 端到端出 HTML 报告；SC-01/SC-02 达标。
**✅ 已达成（2026-09-10）**：T201-T223 全部 ☑；`mvn -B -o clean verify` **331 个测试全绿**（2 个按需联网的默认 skip）；
SC-02 见 T220/T221（同输入三次逐位一致，且 26k 根上三个独立 JVM 进程的 HTML sha256 相同）；SC-01 见 T221（**26281 根 / 回放 425ms / 整个 JVM 1.379s**，预算 5 分钟）。

> 设计取舍记录（与 DESIGN 的非冲突细化）：
> 1. DESIGN §7.2 提到 Spring 扫描 `@StrategyComponent`。实现改为 **`StrategyFactory` SPI（`META-INF/services` 发现）+ app 侧按 yml 装配**，
>    以保持 alpha-strategy 模块零 Spring 依赖（策略可脱离容器单测）。新增策略只需：策略类 + 工厂类 + services 一行 + yml 一条，
>    引擎/风控/OMS/app 装配代码零改动（场景 6、NFR-06）；分发顺序取 yml 顺序而非 ServiceLoader 发现顺序，保证可复现。
> 2. K 线仓储契约放 alpha-common（纯接口，零依赖），SQLite/MySQL 的 JDBC 实现放 alpha-app（唯一有 Spring/驱动的模块），
>    CSV 实现放 alpha-backtest —— 严格守住 `app → backtest → {strategy,risk,execution,gateway} → engine → common` 单向依赖。
> 3. `Portfolio`（持仓/权益/已实现盈亏）放 alpha-common：它是 spec §5 的领域实体，回测撮合与实盘 OMS 必须共用同一份实现（FR-BT-06）。
> 4. **P2 的风控范围是刻意收窄的，不是遗漏**：`RiskGate` 目前只做仓位换算（FR-RK-07）+ 交易所最小值校验（边界 4），
>    DESIGN §8 的账户/单笔/组合/熔断/频率规则在 P3 以有序管道接在它前面，下游（OMS/撮合）无需改动。
>    同理 `PositionSizer.Policy.targetExposure` 被限制在 (0, 1]：在账户级杠杆规则（FR-RK-02/04）就位前，
>    sizer 不允许单靠自己把敞口放大到超过权益，上限放开必须与那两条规则同一个 PR 落地。
> 5. **`JdbcKlineRepository` 的 MySQL 兼容目前是「构造上成立」，不是「实测通过」**：同一份方言中立 DDL（不用保留字、
>    `VARCHAR`/`BIGINT`/主键三元组）、价格一律存**文本**（SQLite 的 NUMERIC 亲和性会把 `'36498.60'` 存成 double，
>    MySQL 的 `DECIMAL(24,8)` 会补零成 `36498.60000000`，两者都会破坏逐位复现；这些列上没有任何 SQL 运算），
>    `save` 的「新增/变更条数」由内存 diff 得出（MySQL 报*已修改*行数、SQLite 报*匹配*行数，驱动的返回值都答不了这个问题）。
>    但测试只跑了 SQLite，`mysql-connector-j` 也刻意尚未加入依赖 —— 等 P3/P4 真正需要 MySQL 持久化时再补集成测试。
> 6. **T219 的配置面按「是否回测专属」划分，而不是照抄任务文字**：任务写的是「`alpha.backtest.*` 配置（数据源、策略启停与参数、
>    初始资金、滑点费率）」，但**初始资金与策略启停没有搬进 `alpha.backtest`**，仍读共用的 `alpha.initial-cash` / `alpha.strategies`。
>    理由：FR-BT-06 的同构若只覆盖类而不覆盖配置，就能配出一个「回测 10 万、实盘 1 万」或「回测开三个策略、实盘开一个」的系统，
>    而回测结果照旧被当成实盘的证据。`alpha.backtest.*` 只放真正回测专属的东西（数据源、区间、序列、敞口、成本、精度规则、
>    报告目录、journal）；`exposure`/`cost`/`quiescenceTimeout` 保持可空，为空时直接把 `Policy.DEFAULT` /
>    `CostModel.DEFAULT` / `DEFAULT_QUIESCENCE_TIMEOUT` 交给 runner，让那三个数字始终只有一处定义。
> 7. **`alpha.backtest.data.source: db` 目前只接受 SQLite，且缺 `from`/`to` 是拒绝运行而不是猜默认值**：前者是取舍 5 的延续
>    （`mysql-connector-j` 尚未进依赖，给一个别的 URL 只会得到一条 `ClassNotFoundException` 而不是一句人话，所以在装配处就拒并点名属性）；
>    后者是因为区间猜宽了会被 feeder 如实报成数据缺口 —— 一次配置笔误会变成一份声称数据集有洞的报告，比直接失败更贵。
> 8. **T220 的冒烟数据集提交进仓库、窗口写成绝对时间戳，且年化收益永不设阈值**。任务只说「固定数据集 + 阈值断言」，
>    但「固定」有两种做法：滚动窗口（最近 N 天）会让夹具**自己重新测量自己**，阈值随行情漂移；绝对窗口
>    （2024-01-01T00:00:00Z 至 2024-04-09T23:00:00Z，2400 根）则让每个被钉住的数字都是可复核的历史事实，代价是 171KB 进仓库。
>    年化只报不断（取舍 7 的同一逻辑再上一层）：100 天的结果几何外推到一年，T219 那条 60 根的回放算出过 **530622%**，
>    对它设阈值要么宽到什么都没断言，要么每次合法改窗口都失败。**冒烟测试走 `BacktestWiring` 而不是 `BacktestRunner`** ——
>    打包产物跑的正是这条 yml→wiring→runner 的路，直接调 runner 的冒烟会在装配坏掉时继续绿。
>    重新生成夹具的那个测试**需要网络，因此默认跳过**（`-Dalpha.it.testnet=true` 才跑），`mvn verify` 保持离线可跑；
>    它同时也是「夹具可复现」的正面证据（二次下载 stored=0）。
> 9. **T223 是补的，不在原 P2 清单里；下载没有自己的「目的地」旋钮，默认序列也不从策略派生。**
>    补它的原因是产品缺口而不是任务缺口：FR-BT-05 要的是一个*工具*，而 T212 的 `BinanceKlineDownloader` 只有 JUnit 能调到 ——
>    `application-backtest.yml` 把 `csv-dir` 写成「下载器写进来的目录」，却没有任何东西往里写，SC-01 的三年验收也只有写过一次性
>    harness 的人能复现。做法沿用 T219 的退出机制（`ApplicationRunner` + 该 profile 下没有 `EventEngine` bean）。
>    **目的地刻意不设第二个旋钮**：下载写入 `alpha.backtest.data`，与回测走同一个 `DataPlan.store` ——
>    一个目录两处定义正是「下载成功了但回测没有数据」的成因，而它的现象是一份没有交易的报告，读起来像策略问题。
>    这是把取舍 6 的同构原则从「权益与策略」再推进一层到「K 线存储」。
>    **默认序列取全局 `alpha.symbols` × `alpha.interval`，而不像回测那样从启用的策略派生**：拉历史是数据操作，
>    不该要求存在一份合法的策略配置；两条规则允许不同，因为缺数据的序列是响亮的（feeder 报成覆盖整段区间的缺口，边界 3），
>    而多下载一条没人交易的序列只花网络与磁盘。显式 `series` 一旦给出就**替换**而非追加全局那一份。
>    **区间不回落到 `alpha.backtest.from/to`**：下载是累积且幂等的（同区间重跑 stored=0），回测是一次声明的实验，
>    共用区间会让「再下载一次」悄悄改变数据集边界。缺 `from`/`to` 一律拒绝运行（取舍 7 的同一逻辑）。
>    **`alpha.backtest.data` 因此声明在共享的 `application.yml` 而不是 `application-backtest.yml`**：Spring 只为激活的 profile
>    加载 `application-<profile>.yml`，download profile 从不读回测那个文件；把它写在 profile 专属文件里，两个模式一致就只靠
>    record 默认值恰好等于出厂值。`ShippedConfigurationTest` 钉的是「哪个文件声明了它」而不是绑定后的值 —— 后者在
>    「文件里写了」与「回落默认」两种情况下不可区分。
> 10. **SC-01 的三年数据集不提交，CI 里也没有计时断言。** 与取舍 8 里那份 171KB 冒烟夹具不同：冒烟夹具的价值在于
>    每次 `mvn verify` 都跑同一批 bar 并逐位比对结果，必须随代码一起进仓库；SC-01 的价值在于**一个数字**（跑完要多久），
>    而 1.9MB 换不来任何断言 —— 复现它需要的是 T223 的两条命令加数据集 sha256（`b4484e26…`），不是仓库里的副本。
>    不加「< 5 分钟」的 CI 断言同理：实测 **1.379s**，余量两个数量级，这样一条断言在慢 CI 上只会变成抖动源，
>    而它的验收标准本来就写着「实测计时记录在执行日志」。SC-02 反而在 26k 根上顺手加强了一次：三个**独立 JVM 进程**
>    （默认 / 换 report-dir / `journal=true`）产出的 HTML sha256 相同 —— 比 T220 的同进程三次比对更硬。

---

## P3 风控 + OMS 全链路

> 本阶段是系统第一次"碰"交易所的写接口（plan §6）：信号 → 风控闸门 → 下单 → 成交 → 持仓更新。
> **环境事实（沿用 P2，2026-09-10 复核）**：本机可直连 testnet REST/WS，但**没有任何 API Key**
> （用户未提供；FR-SEC-01 要求密钥只走环境变量，仓库零密钥）。因此凡验收需要「真实下单 / 真实成交回报 /
> 真实对账」的部分（T321 全部，T316-T318 的 testnet 实跑部分）**阻塞**：实现照做，验收改为**离线桩**
> （JDK `com.sun.net.httpserver.HttpServer`，因为 `mockwebserver` 不在本地 m2）+ 真实报文样本单测，
> 实跑标记为用户验收项（同 T112 的处理）。**这不影响 SC-04 与场景 3**：风控拦截是纯本地行为。

| # | 任务 | 对应 spec | 验收 | 状态 |
|---|---|---|---|---|
| T301 | `RiskRule` SPI + `SignalFacts`/`OrderFacts` + `RiskPipeline`（有序、任一拒绝即短路、两阶段，见取舍 11）；`RiskGate` 改为管道宿主，回测行为不变。原计划的单一 `RiskContext` 拆成两个事实记录：一个上下文类型会让「数量尚不存在」这个状态可表达 | RK-01 | 管道顺序/短路/两阶段单测；`mvn -B -o verify` 全绿且 `BacktestSmokeTest` 钉住的数字不变 | ☑ |
| T302 | `AlphaProperties.Risk`：五层参数 + 每层 `enabled` 开关，声明在**共享** `application.yml`（取舍 13） | RK-09 前置、OP-01 | 绑定单测（缺省值逐条等于 DESIGN §8 表）+ `ShippedConfigurationTest` 扩展「哪个文件声明了它」 | ☑ |
| T303 | 账户级规则：总杠杆上限（默认 3x）、保证金率下限（默认 150%，定义见取舍 12） | RK-02 | 阈值两侧（恰好等于 / 超出一分）单测 + 变异测试 | ☑ |
| T304 | 订单级规则：单笔名义价值上限（默认 ≤20% 权益）、价格偏离最新价上限（默认 ≤2%，防乌龙指） | RK-03 | 限价单偏离拦截 + MARKET 单无价格时**跳过偏离检查**的语义单测 | ☑ |
| T305 | 组合级规则：总持仓名义上限（默认 ≤60% 权益）、单 symbol 上限（默认 ≤30%） | RK-04 | 「本单成交之后」口径的拦截单测（含减仓单永不被这两条拦） | ☑ |
| T306 | 熔断级：单日亏损达阈值（默认 -5%）当日禁止开仓 + 连亏 N 笔（默认 3）暂停 M 小时（默认 2h）；`CircuitBreaker` 观察 `FillEvent` 与 UTC 日界 | RK-05 | 日界翻转 / 连亏计数 / 暂停到期单测（VirtualClock 定点推进） | ☑ |
| T307 | 频率级：单位时间最大订单数（默认 ≤10 单/分钟，滑动窗口） | RK-06、SC-04（取舍 14） | 窗口滑动 + 突发拦截 + 窗口过期后恢复单测 | ☑ |
| T308 | **SC-04 拦截矩阵**：五级规则各 ≥1 条「必被拦截」用例，拦截率 100%，并断言**短路点**（命中的是哪一条 ruleId） | SC-04 | 矩阵测试逐条列 ruleId；逐条删掉每个 guard 必须有用例失败（变异测试） | ☑ |
| T309 | 持久化契约：`OrderStore` / `RecordStore` 接口（execution / risk 各自定义，零 Spring 零驱动）+ 回测用内存实现 | OP-04、RK-08、EX-01 | 接口编译 + 内存实现 round-trip 单测；红线 grep：alpha-risk/execution 无 jdbc、无 spring | ☑ |
| T310 | 业务库 DDL（`orders`/`fills`/`signals`/`equity_snapshot`/`positions`/`risk_interceptions`，方言中立、价格与数量存文本，见取舍 15）+ `JdbcOrderStore`（alpha-app） | OP-04、EX-01 | 临时 SQLite 文件 round-trip 单测；建表幂等（重复启动不报错） | ☑ |
| T311 | `JdbcRecordStore`：signals / risk_interceptions（含命中规则 + 当时账户快照）/ equity_snapshot / positions | RK-08、OP-04 | round-trip 单测 + 「按规则/时间查询拦截记录」单测 | ☑ |
| T312 | OMS 订单状态机：NEW→SUBMITTED→PARTIALLY_FILLED→FILLED/CANCELED/REJECTED，每次迁移写库 + 发 `OrderUpdateEvent` | EX-01 | 全路径单测（含**非法迁移一律拒绝**：终态不可再迁移） | ☑ |
| T313 | clientOrderId 幂等 + **边界 6**：区分「交易所拒单」与「交易所不可用」——后者留给对账，**绝不判失败**（重试归网关，见取舍 20） | EX-02、边界 6 | 重复提交不产生重复订单；传输异常不推进状态机的单测 | ☑ |
| T313b | **补 T309 的装配硬约束**：`RiskGate` 注入 `RecordStore`，在 `block()` 里写 `InterceptionRecord`（`SignalFacts` + `RiskRejection`）。不能挂在总线上做——`SignalFacts` 只存在于一次 `onEvent` 调用内部、从不发布，任何 handler 注册顺序都还原不出它 | RK-08、OP-04 | 五级规则各一条「库里查得回、且字段与同时发出的那条告警一致」的单测；T320 依赖它 | ☑ |
| T313c | **`signals` 表至今没有写入方**：`RecordStore.saveSignal` 在生产代码里零调用者（T313b 只补上了 `saveInterception`，`saveEquitySnapshot`/`savePosition` 归 T318b）。补一个 `SignalRecorder implements EventHandler`（alpha-risk），在总线上落 `SignalEvent` 行。**这一条与 T313b 相反、必须挂在总线上做**，但理由不是「有些信号到不了闸门」——`EventEngine.dispatch` 逐 handler 捕获异常后继续，注册了的 handler 一定看得到每一个事件。真正的理由有两条：①**闸门抛异常时它自己一个字都不留**（规则里的 bug 被 dispatch 吞掉，既没有告警也没有拦截行），而注册在闸门之前的 recorder 已经写下了那个信号，那是唯一能证明「当时在飞的是什么」的行；写进闸门就等于把「策略发了什么」定义成「风控成功收到了什么」。②信号是**总线事件本身**，不像 `SignalFacts` 只存在于一次调用内部，所以挂观察者什么都不损失。**且必须注册在闸门之前**：两张表按 `event_id` join，顺序决定 join 的哪一半会悬空——先写信号，两次写入之间挂掉只留下「一个没被拒的信号」；先写拦截，则留下一条 `signal_event_id` 指向不存在行的拦截记录，正好是这对表存在意义所在的那个查询坏了 | OP-04、RK-08 | 「拦截记录能 join 回它拦的那个信号」的单测（同一 `eventId`）+ 写入顺序单测（用一个记下写入次序的 `RecordStore` 装饰器，否则注册顺序不可观测）+ **闸门抛异常仍留下信号行**的单测（真 `EventEngine`，且断言规则确实被咨询过，否则该用例会因「闸门根本没被调用」而空过）；T321 的「每笔信号可在库中查询」依赖它 | ☑ |
| T314 | 成交回报处理：OMS **先写 `Portfolio` 再 publish `FillEvent`**（取舍 16）+ 落库 | EX-06、场景 2 | 部分成交 / 多次成交 / 翻仓的账本与事件序单测 | ☑ |
| T315 | 精度对齐：启动拉 `exchangeInfo` → 缓存 tickSize/stepSize/minNotional 的 `TradingRulesProvider` 实现 | GW-03 | 脏单被拒测试；T209 的「规则缺失 = CRITICAL 硬停」不被新实现绕过 | ⏳ 进行中：`BinanceExchangeInfoParser` / `BinanceTradingRulesFetcher` / 测试已写在磁盘（182+183+252 行，可编译），**尚未 git 提交、尚未接进在线装配**——当前 `TradingRulesProvider` 只有回测用的 `FixedTradingRulesProvider` |
| T316 | Binance 签名 REST：HmacSHA256 + `TimeSync` 偏移 + 下单/撤单/账户/持仓/挂单；**边界 8** 时钟漂移超阈值 → 告警并暂停下单 | EX-03 前置、GW-05、边界 8 | 离线桩：签名与测试内**独立算出**的 HMAC 逐字节相同；漂移守卫单测；密钥只从环境读 | ☐ |
| T317 | user data stream：`listenKey` 创建 + **30min keepalive** + `ORDER_TRADE_UPDATE` 解析 → 发 `OrderReportEvent`（取舍 19：交易所事实只从这一个入口进 OMS，`FillEvent`/`OrderUpdateEvent` 由 OMS 出）；REST 查询兜底。**`OrderReportEvent` 必须带上交易所的成交 id（Binance 的 `t`），OMS 按 `(clientOrderId, tradeId)` 去重** —— 重连流重投的成交报文与真正的第二笔在 OMS 眼里完全相同（PARTIALLY_FILLED→PARTIALLY_FILLED 是 T312 刻意留下的唯一合法自环），没有成交 id 就会重复计钱；终态那侧已经安全（FILLED 无后继，T314 的 `aDuplicatedTradeOnAFilledOrderMovesNeitherTheRowNorTheBook` 钉住），漏的正是自环这一侧 | EX-03、EX-06 | 报文解析单测（真实 testnet 报文样本）+ keepalive 定时单测（VirtualClock）+ **同一成交 id 重投不重复计钱**的单测；实跑 ⊘ 需 Key | ☐ |
| T318 | 定时对账（默认 60s）：挂单 + 持仓 + 账户，以交易所为准修正本地，差异发 `RiskAlertEvent`；启动先全量同步；幽灵仓位**仅告警**（自动平仓属 P4，取舍 18） | EX-04/05、边界 2/7 | 人为制造三类不一致（挂单丢失 / 持仓不符 / 幽灵仓位）均被修正并告警的单测（假网关） | ☐ |
| T318b | 快照采样器：定时写 `equity_snapshot` + `positions`（`RecordStore` 四张表里唯一需要采样、而不是总线上已有事实的两张）。持仓行必须带**当时的 `markPrice`** | OP-04、EX-06 | 采样单测（VirtualClock）：平掉的 symbol 按 `Portfolio.openPositions()` 落行；「用后来的标记价重估过去的持仓」必须不可能 | ☐ |
| T319 | paper/live 装配收口：OMS / 对账定时器 / 持久化 / 网关凭据按 profile 装配，**每个在线 bean 都带 `@Profile({"paper","live"})`**；backtest 与 download 的自动退出不得被破坏。**`StartupWiring` 目前只注册了 `StrategyEngine`，本任务要把 `SignalRecorder` → `RiskGate` → OMS 接进这条序列，而注册顺序就是 dispatch 顺序**：`SignalRecorder` 必须在 `RiskGate` 之前（T313c 的 `theSignalRowIsWrittenBeforeTheInterceptionRowThatNamesIt` 钉住了原因——反过来则两次写入之间挂掉会留下一条 `signal_event_id` 悬空的拦截记录），`SimulatedExecutor` 的位置在线上由 OMS 取代（取舍 16） | OP-01、SEC-01 | `PaperProfileApplicationTest`（假网关，容器可启动可关闭）+ 既有两个 profile 容器测试仍绿 + **断言 handler 注册顺序**（recorder 在 gate 之前，否则这条约束只能靠 code review 守） | ☐ |
| T320 | **场景 3 风控硬闸门**端到端（离线）：超限信号被拦、不发往交易所、产生告警事件、拦截记录可查（含命中规则与当时账户状态） | 场景 3、RK-01/08 | 端到端测试用假网关断言 `placeOrder` **从未被调用** + 从库里查回拦截记录 | ☐ |
| T321 | **场景 2 模拟盘全链路**：testnet 实时行情 → 信号 → 风控 → 下单 → 成交回报 → 持仓/净值更新，且每笔信号/订单/成交可在库中查询 | 场景 2、EX-03/06 | ⚠️ 需 testnet API Key，标记为用户验收项 | ⊘ 阻塞 |

**P3 出口**：T301-T320（含补挂的 T313b / T313c / T318b）全部通过且 `mvn -B -o clean verify` 全绿；spec 场景 3 逐条验收通过；
SC-04 拦截率 100%；场景 2（T321）由用户在有 testnet Key 的环境执行。

> 设计取舍记录（续 P2 的 1-10）：
> 11. **风控管道分 PRE_SIZE / POST_SIZE 两阶段，不是一个平面列表。** DESIGN §8 只说「管道串联、任一拒绝即拦截」，
>    但订单级（单笔名义上限）与组合级（总仓 / 单 symbol 上限）**必须先有数量才能评估**，而数量正是 FR-RK-07 换算出来的。
>    于是：账户级 / 熔断级 / 频率级在换算**之前**跑（它们只看信号与账户状态，提前短路还省下换算），
>    订单级 / 组合级在换算**之后**跑，且按「这一单成交之后」的口径评估。把五条规则塞进一个平面管道，
>    得到的订单级检查会因为 `qty == null` 而恒真（或恒假），而且**没有任何测试能发现**——它会安静地放过每一单。
> 12. **`marginRatio` 用本系统自己的定义，绝不用交易所那个同名字段。** DESIGN §8 的「保证金率 ≥150%，低于阈值禁止开新仓」
>    是证券语义：权益 / 占用保证金，**越大越安全**；而 Binance 的 `AccountSnapshot.marginRatio` 是维持保证金 / 保证金余额，
>    **方向相反**（越大越接近强平）。把网关那个字段直接喂进这条规则，会让「快要爆仓」通过检查、「非常安全」被拦死，
>    而且两种情况都看起来像正常拒单。故规则自己算：`usedMargin = totalNotional / maxLeverage`，`marginRatio = equity / usedMargin`。
>    在 `maxLeverage=3x` 时它等价于 `totalNotional ≤ 2×equity`，**比杠杆上限（≤3×equity）更紧** —— 这正是它存在的意义：
>    把 maxLeverage 调大不会顺手把全部缓冲删掉。交易所的 marginRatio 只进对账告警（T318），永不进这条规则。
> 13. **风控参数进共享 `application.yml` 的 `alpha.risk.*`，不新建 DESIGN §8 说的 `risk-rules.yml`。**
>    这是取舍 9 那条理由的第三次应用：回测与实盘必须读到同一套规则（FR-BT-06），而 Spring 只为激活的 profile 加载
>    `application-<profile>.yml`；再多一个独立文件就多出「哪个文件赢了」的问题，而它的失败现象是
>    「回测里被拦住的信号，实盘照发」—— 回测因此不再是实盘的证据。热更新（RK-09，P4）不需要独立文件：
>    改的是内存里的规则集，REST 端点照样能做，且那样才谈得上「不重启」。**与 DESIGN 的文件名不一致，记在此处。**
> 14. **频率级（RK-06）在 P3 做，尽管 plan §2 把它划给 P4。** SC-04 写的是「覆盖**全部五级规则**的拦截测试用例，
>    拦截率 100%」，而 P3-2 的验收正是 SC-04；plan §6 的任务文字自己也写着「五级规则实现」，只是 spec 映射列写了 RK-02~05。
>    少一级就拿不到 SC-04，而这条规则是一个滑动窗口计数器（≤10 单/分钟），成本远低于它换来的验收完整性。
>    plan §2 与 §6 的这处不一致按「§6 + SC-04」解决。
> 15. **DESIGN §11 的四张表在 P3 扩成六张。** `orders`/`fills`/`signals`/`equity_snapshot` 之外加 `positions` 与
>    `risk_interceptions`，因为 spec 直接要求的两件事在四张表里无处可放：FR-RK-08「拦截记录可查询，包含命中的规则与
>    当时账户状态」（场景 3 的 And 子句）与 EX-04/EX-06 的持仓修正需要一份可查的持仓历史。把拦截塞进 `signals`
>    会让一张表同时表达「策略想做什么」和「风控不让做什么」两种事实，查询口径只能靠一列 flag 区分。
>    schema 保持方言中立、价格与数量**存文本**（取舍 5 的同一理由：SQLite 的 NUMERIC 亲和性与 MySQL 的 `DECIMAL`
>    补零都会破坏逐位复现）；MySQL 集成测试仍留到 P4-8。
> 16. **OMS 占的正是回测里 `SimulatedExecutor` 的那个位置，并且由它写账本。** paper/live 注册 `OrderManager`、
>    backtest 注册 `SimulatedExecutor`，两者吃同一个 `OrderRequestEvent`、发同一个 `FillEvent`，链路其余部分一行不改
>    （FR-BT-06 的第三次兑现：类同构 → 配置同构 → 链路位置同构）。写账本的规则沿用 T215 的结论 ——
>    **谁确知成交发生，谁就在 publish `FillEvent` 之前把成交写进 `Portfolio`**：这样「账本 handler 必须排在策略之前」
>    这条隐式注册顺序约束根本不存在，且策略 `onFill` 看到的持仓就是这笔成交造成的。
> 17. **出厂默认值必须自洽：`targetExposure` 与「单笔 ≤20% 权益」是耦合的，耦合式是 `单笔上限 ≥ 2 × targetExposure`。**
>    翻仓那一单的数量是目标仓位的两倍，所以任何 flip 的名义都是 `2 × targetExposure × equity`；
>    而 `PositionSizer.Policy.DEFAULT` 现在是 0.30 —— 配上 DESIGN 的 20% 上限，**所有翻仓、以及所有从空仓建到 30% 的单
>    都会被订单级规则拦掉**。规则没错，错的是两个默认值放在一起不成立（P2 没有订单级规则，所以这个矛盾一直不可见）。
>    P3 落地时把出厂 `targetExposure` 降到 **0.10**（上限 0.20 恰好容纳一次翻仓），并在 `application.yml` 注释里写明这条不等式。
>    **`BacktestSmokeTest` 必须显式声明一套不咬人的风控参数**：它钉的是数据集 + 策略 + 撮合的确定性，不是风控默认值，
>    让它继承出厂风控参数等于把两件事的变更绑在一起。若钉住的数字仍然变了，按 `PROVENANCE.md` 在同一个提交里重新测量。
> 18. **两处"P3 只做一半"是刻意的，不是遗漏。** ①RK-08 的「推送通知」在 P3 只到**事件 + 落库 + 日志**为止：
>    邮件通道是 P4-1（JavaMailSender + QQ SMTP，需要 SMTP 授权码，当前阻塞）。P4 接上推送时不需要改风控层一行。
>    ②EX-05 幽灵仓位在 P3 **只告警不平仓**（plan 把自动平仓划给 P4-5）：自动平仓是一个会自己下单的行为，
>    在没有 7 天浸泡证据之前，不该跟对账同一个 PR 落地 —— 对账判错一次，代价就是一笔真实的反向单。
> 19. **OMS 的入口是新增的 `OrderReportEvent`，不是复用 `OrderUpdateEvent`。** 交易所关于订单的事实（ack / 拒单 / 撤单 /
>    逐笔成交）**必须以事件进循环**，不能从 socket 线程直接调 OMS：`EventHandler` 的契约禁止在引擎线程上做网络 IO，
>    而 `OrderStore` 靠「只有引擎线程写它」才不需要加锁，`EventEngine.publish` 是唯一的线程安全入口。
>    但**入口不能用 `OrderUpdateEvent`** —— 那是 OMS 每接受一次迁移就对外发布的事实，同一类型双向使用会让 OMS
>    收到自己的发布、把每条都当成关于该订单的新报告，于是每次迁移在 journal 里留两条，而重连流里的重复报告
>    从此与自己的回声无法区分。两个类型也正是「交易所告诉我 X」与「系统现在知道 X」在 journal 里的可区分性，
>    事后诊断乱序与重复报告靠的就是它（FR-EX-01/03）。**一条记录两种形态**（生命周期报告带 `status` 不带成交，
>    成交报告带 `lastQty/lastPrice/fee` 不带 `status`），因为一条交易所报文就是这两者之一；成交报告**不带 status**，
>    是「现在算部分成交还是全部成交」由订单自己的数量决定（OMS 从行里推），而不是信一个可能与它旁边的数字互相矛盾的字段。
>    数量是**本次成交的**而不是累计的：累计值一旦也带上，同一个数就有了两个来源，每次不一致都得在最没能力判断的地方做取舍；
>    漂移由 FR-EX-04 的对账负责，它才是那个去问交易所「你到底持有什么」的组件。T313 的发送方、T317 的流解析、
>    T318 的对账共用这一个入口。
> 20. **spec §6.6 的「后者重试而非判定失败」拆成三件事，分别由三个组件负责；发送方刻意不重试。**
>    ①**区分**放在网关边界上，而且是一个**类型**而不是一条错误码：`ExchangeGateway.placeOrder` 的契约是
>    「有回答就返回 `OrderAck`（拒单也是回答），没回答就抛 `ExchangeUnreachableException`」，
>    绝不允许用「返回一个默认值 / 空值」表示不可达 —— 不可达时返回 `OrderAck.rejected` 正是 §6.6 要防的那件事，
>    而返回空挂单列表会让「以交易所为准」的对账把本地状态修正成一个虚构（网关 javadoc 里写死了这条）。
>    ②**响应**放在 `OrderSender`：不可达 → 一条 CRITICAL 告警 + 订单行**原地留在 NEW** + 交给对账，
>    一行状态都不推进（这就是 T313 的验收「传输异常不推进状态机」）。两个未知原因用两个 ruleId
>    （`EX-exchange-unreachable` / `EX-send-failed`），因为「交易所没接」与「我们的代码炸了」修法不同、责任方也不同，
>    但对二者的**处置必须完全一致**，所以那句结论只写在一个地方（`unknownOutcome`）—— 两个原因写成两段结论，
>    迟早漂成两种处置。③**重试**放在网关实现里（T316），**不在发送方**：只有真正握着连接的那一层知道请求有没有
>    写到线上，而「不知道有没有写出去」时重发是**唯一能把幂等变成重复下单的做法** —— 第一次其实已经落地的
>    `clientOrderId` 重发回来是一条**拒单**，而这条拒单与真正的拒单在发送方眼里一模一样，于是它会把一笔活着的挂单
>    判成死单，策略再次发信号，交易所上就有了两笔。所以重试只在「确知从未上线」时安全（连接建立失败、写不出去），
>    而那个判断只有网关做得了；重发必须复用 `request.clientOrderId()`（FR-EX-02），让交易所去拒绝重复而不是成交两次。
>    **发送方也不清 `handedOver`**：一旦某个 id 可能被交易所见过，再发它就正是 FR-EX-02 禁止的事；
>    真要重下的单是一张**新单**，从 `ClientOrderIds` 取一个新 id。
>    **代价**：一次不可达意味着这张单在下一轮对账（默认 60s，T318）之前一直悬着，策略看到的仓位与交易所可能不一致
>    最长 60s。换来的是一笔重复下单也不会发生 —— 在这个系统里前者是可恢复的偏差，后者是真实的钱。

---

## 执行日志

| 时间 | 任务 | 结果 |
|---|---|---|
| 2026-09-09 01:00 | T001-T010（P0 全部） | 通过：mvn clean verify 全绿；三 profile 可启动；live 缺密钥 fail-fast 正确；双日志文件生成；CI workflow 就绪 |
| 2026-09-09 01:02 | T101-T106（事件模型+引擎+日志） | 通过：9 个单测全绿（顺序性/连锁闭环/定时器/异常隔离/round-trip）。修复 1 个真实 bug：定时器注册无法唤醒 parked 引擎 |
| 2026-09-09 01:03 | T107-T110（网关+Binance 行情） | 通过：parser/backoff/timesync 单测全绿；交易接口 P3 占位 fail-fast |
| 2026-09-09 01:04 | T111（装配验收） | 通过：paper 模式实测连接 stream.binancefuture.com 并接收 BTCUSDT 实时 K 线事件 |
| 2026-09-09 01:04 | T112（断网 60s 恢复） | ⊘ 跳过：需用户网络环境执行，标记为用户验收项 |
| 2026-09-09 01:05 | 提交推送 | commit dc8a55c 已推送 origin/main，触发 GitHub Actions CI |
| 2026-09-09 01:40 | P2 细化 | 生成 T201-T222（22 条），并实测环境：fapi.binance.com/repo.maven.apache.org 不可达，testnet REST 可直连（含 2019 至今 BTCUSDT 1h）；已在 `~/.m2/settings.xml` 镜像 central → repo1.maven.org（仅本机，CI 不受影响） |
| 2026-09-09 01:43 | T201-T202（精度模型 + 组合领域模型） | 通过：alpha-common 18 个单测全绿（取整/对齐/minNotional 拒单、加减翻仓均价与实现盈亏、资金费多空方向、确定性 scale 一致） |
| 2026-09-09 01:52 | T203-T205（Strategy SPI + BarSeries + 指标库） | 通过：alpha-strategy 21 个单测全绿。指标值以独立 Python 参考实现（教科书公式：EMA 以 SMA 播种、RSI/ATR 用 Wilder 平滑）逐值钉死；BarSeries 覆盖重复/乱序丢弃与容量淘汰（边界 1）。修正 1 处测试自身错误（EMA 播种窗口取前 5 根而非后 5 根） |
| 2026-09-09 02:00 | T207-T208（MA 双均线 + RSI 反转策略） | 通过：新增 12 个单测全绿（alpha-strategy 累计 33）。信号只在状态跃迁的那根 K 线发出，超买/超卖持续多根不重复下单；RSI 平仓以「已请求平仓的持仓数量」去重，既避免逐根重复发信号，也保证迟到成交重新建立多头后仍会被平掉；Strategy SPI 补 `interval()`（单周期，多周期不在 v1 范围）。修正 1 处测试自身错误（每根 K 线新建策略实例导致指标永不预热） |
| 2026-09-09 02:06 | T206（StrategyEngine 分发器） | 通过：新增 8 个单测全绿（alpha-strategy 累计 41）。分发规则：只投 closed bar（未收盘永不进策略，FR-BT-02）、按 symbol+interval 双维度过滤、同 symbol/interval 的多策略共用一份 BarSeries 且每根只追加一次（重复/乱序直接丢弃且不回调策略，边界 1）、按配置顺序分发保证可复现、单策略抛异常被隔离不影响其他策略 |
| 2026-09-09 02:15 | T222（策略工厂注册 + app 装配） | 通过：新增 19 个单测（alpha-strategy 累计 60，全仓 84 全绿）。`StrategyFactory` 走 `META-INF/services` 发现，`StrategyRegistry` 按 yml 顺序构建；配置错误一律启动即失败（未知 type/重复 id/参数拼错/参数值非法/工厂忽略配置 id/两个工厂抢同一 type）。app 侧：删除 EchoStrategy 占位，新增 Portfolio 与 StrategyEngine bean，网关订阅改为「alpha.symbols ∪ 已启用策略声明的 symbol+interval」，避免新策略因无人订阅而静默空跑。实测：backtest profile 启动日志正确显示发现/启用/停用；paper profile 连上 stream.binancefuture.com 并订阅 BTCUSDT.PERP/1h |
| 2026-09-09 02:28 | T209（PositionSizer + 风控直通闸门） | 通过：新增 27 个单测（alpha-risk 26 + alpha-common ClientOrderIds 5，另 StrategyEngineTest 补 1 条标记价断言；全仓 126 全绿）。四处理解决策：①金额精度抽到 `common.model.Money`（MC(24,HALF_UP)/scale 8）唯一定义，Portfolio 与 sizer 共用，避免两处各自取整导致回测与实盘漂移（数量断言按 scale 精确比对而非 compareTo）；②P2 的 `targetExposure` 上限钉死 1.0，杠杆/账户级敞口是 P3 的 FR-RK-02/04，规则未就位前不允许 sizer 自行放大账户；③闸门只发 MARKET 且 `price=null`——回测撮合按次根开盘、实盘按盘口，此处带限价要么是未来函数要么是过期报价；④`StrategyEngine` 在分发前用收盘 `portfolio.mark()`，否则 paper/live 每个信号都会因无标记价被 `RK-07-no-price` 拦死（回测因撮合先跑不易暴露）。边界 4 落实：不足 minNotional / stepSize 取整为 0 一律拒单 + WARNING 告警，绝不发脏单；无交易规则视为 CRITICAL 硬停 |
| 2026-09-09 23:51 | T210-T212（历史数据层：契约 + CSV + JDBC + Binance 下载器） | 通过：新增 39 个单测（backtest 12、app 10、gateway 22 含 1 条按需 testnet IT；全仓 165 全绿）。契约决策：`KlineRepository.load` 必须返回**按 openTime 升序、每个 openTime 一根**，`save` 必须**幂等**（可重复覆盖同一区间），价格**原样存原样还**（绝不二次取整，NFR-04）——三条不变式让 CSV/SQLite/MySQL 对 feeder 与策略完全等价，T214 的缺口检测可以直接信任「一根一根」。`CsvKlineRepository` 走 `.tmp` + `ATOMIC_MOVE`（不支持则退化为普通 move），`changed==0` 时不落盘；损坏行报 `文件:行号`。`JdbcKlineRepository` 见取舍 5（建表自动执行，价格存文本，内存 diff 决定「新增/变更」计数）。`BinanceKlineDownloader`：1500 根分页、base url testnet/实盘可配（FR-GW-05）、429/418/5xx 退避重试并优先听 `Retry-After`、4xx 立即失败不重试；**绝不入库未收盘的那一根**（以 Clock 判定 `closeTime < now`），否则一个「其实从未存在的收盘价」会在之后每次回测里静默违反 FR-BT-02。实测 testnet 真实拉取 BTCUSDT.PERP 1h 近 100 天：`pages=2 fetched=2400 stored=2399 forming=1`，无缺口，全部满足 OHLC 与 `closeTime=openTime+1h-1` 不变式；公开端点无需密钥，FR-SEC-01 不受影响 |
| 2026-09-10 00:11 | T213（`EventEngine.awaitQuiescence` 回放屏障） | 通过：新增 5 个单测（alpha-engine 7→12，全仓 170 全绿）。屏障语义：`completedThrough >= eventId`（事件 id 全局单调，故用一个 long 水位即可精确表达「该轮及其之前的轮都已闭环」，无需记录 id 集合）**且**引擎空闲（无轮在飞 + 外部队列空 + 无到期定时器）。定时器那一条是关键：资金费这类周期性结算靠定时器驱动，而定时器恰好在 feeder 拨钟时到期，屏障若提前返回就会让 feeder 带着未结算的资金费推进。`false`（超时/引擎已停）必须中止回放，绝不可忽略。**顺带修掉两个真实缺陷**：①`pollNext` 原先按「距下个定时器的毫秒数」park，而该期限取自 Clock——`VirtualClock` 被回放方一次性拨快后，循环会按*真实*时间 park 数小时，已到期的定时器迟迟不触发；改为 park 上限 20ms（有事件时 poll 立即返回，无额外开销）。②`emitDueTimers` 原先写在 try 内，被 `wakeLoop()` 的中断打断时会整轮跳过定时器评估；移出 catch 之外，任何一次唤醒都重新评估。**用变异测试验证断言不是空转**：逐条删掉 `!dispatching`、`millisUntilNextTimer() > 0` 后对应用例必须失败——第一版用例曾在此处假绿（cascade 微秒级排空，靠运气通过），改为在 cascade 中途卡住 handler 后才真正钉死；同时删掉因此冗余的 `cascade.isEmpty()`（`dispatching` 已覆盖整轮）与一条被更强用例包含的弱用例 |
| 2026-09-10 00:28 | T214（`BacktestDataFeeder` 回放 + 缺口检测） | 通过：新增 14 个单测（alpha-backtest 12→26，全仓 184 全绿）。回放语义：每根 K 线严格「拨钟 → publish → `awaitQuiescence`」，一根 == 一轮；不等闭环就会让下一根的事件与本根 signal→risk→order→fill 连锁交错，同一份数据不再可复现（NFR-04）。handler 收到的就是实盘 WS 会给的那个 `KlineEvent`（`closed=true`、businessTs=该根收盘时刻），下游无法分辨回测与实盘（FR-BT-01/06）。多序列**按业务时间归并**（稳定排序），同一 closeTime 并列时按**配置顺序**而非字母/哈希序，多标的交错与实盘一致。缺口三形态统一为一条扫描：起始晚于区间、中间空洞、**数据早于区间结束（尾部缺口）**——三者都计入报告（边界 3）；`Gap` 去掉 `actualOpenTime`（恒等于 `expected + missing*step`，冗余且令尾部缺口无法表达）。**网格校验锚定 `fromOpenTime` 而非只比相邻差值**：只比差值会放过「自身等距但整体错位」的序列，随后前导缺口被 floor 截断，少报缺根数——这条是写完初版后自查发现的真实计数缺陷。空区间**拒绝回放**而不是回放 0 根：0 根回测产出平直净值曲线，读起来像「策略没赚钱」而不是「没有数据」。仓库若返回非严格升序或重复根直接抛（契约已声明），否则重复根会被回放两次并被误报为缺口。**变异测试：8 个变异体杀掉 7 个**（尾部缺口、锚定网格、升序校验、缺口记录、空区间拒绝、引擎未启动拒绝、时间排序）；唯一存活的是「把拨钟挪到 publish 之后」——它确实引入竞态（handler 可能读到上一根的钟，令恰好在该收盘到期的资金费定时器被屏障漏判、错配到下一根），但两条语句相邻，错误顺序几乎每次都读对，**任何黑盒时序测试都抓不住**；因此不假装有测试钉住，改为在 `advanceAndPublish` 现场用 JMM happens-before（advanceTo → queue offer → loop poll synchronizes-with）把「为什么这个顺序不能反、反了会怎样」写死在代码里。另修掉测试自身两处缺陷：`feeder()` 覆盖 `engine` 字段导致先建引擎的非守护线程泄漏（用例全绿后仍可能挂死 surefire fork）；一条 `assertThat(seen).isEmpty()` 是空断言（该引擎根本没注册记录 handler，永不可能失败），换成 `handlerEntries == 1`，真正证明 feeder 停在第一根、没有把其余 4 根堆在打不开的轮后面 |
| 2026-09-10 00:48 | T215（`SimulatedExecutor` 模拟撮合 + 滑点/手续费/资金费） | 通过：新增 22 个单测（alpha-backtest 26→48，全仓 206 全绿）。撮合语义：订单在**下一根 closed bar 的 open** 成交，未收盘根永不成交（FR-BT-02）；成交根自己的 high/low/close 完全不进价格，用例专门喂了一根 open=210、high=999、low=0.5、close=500 的极端根来钉死这一点。**滑点取自「下单那根」的振幅，而不是「成交那根」**：成交根的 high/low 在它自己 open 那一刻是未来信息，用它算滑点是最容易写错、错了以后回测还会系统性偏乐观的一处未来函数；实现上把下单根的滑点算好存进 `Pending`，成交时直接用。偏置一律对交易者不利（买 CEILING、卖 FLOOR、手续费按滑后价计），回测宁可信差不可信好。资金费按 **UTC 对齐的 8h 边界**数据驱动计提，而非 `scheduleRepeating` 定时器：边界因此与「何时开跑」无关，且一段数据空洞会把交易所真的结算过的每个边界全部补上（用例：24h 空洞 → 3 次计提），同时第一根之前绝不追溯计提（否则从 epoch 起算会凭空吞掉权益）。边界 4 的成交侧：滑点后名义再查一次 minNotional，不够就拒，绝不发脏单。**谁把成交写进账本**这一处架构选择记在此：executor 自己写，且在 publish `FillEvent` **之前**——与实盘 OMS 同构（账本算术回测实盘同一套，FR-BT-06），顺带消掉「账本 handler 必须排在策略之前」的隐式注册顺序约束，并保证策略 `onFill` 看到的是这笔成交真实产生的持仓（变异体「先 publish 再 applyFill」被用例抓住）。成交时刻缺交易规则按 CRITICAL 硬停而非普通拒单：撮合下去等于凭空发明一个 tickSize。`pendingOrders()` 在收尾非空即进报告——最后一根的信号没等到成交，这是事实，不能静默丢弃。**变异测试：16 个变异体全部杀掉**，含两条未来函数（次根收盘成交、滑点取成交根）、两条偏置方向反转（取整/滑点偏向交易者）、手续费按原始 open 计、minNotional 复查移除、非 MARKET 接受、非正数量接受、缺规则忽略、资金费锚点移除与「隔一个边界计提一次」、标记价不更新、LIFO、跨标的成交。 |
| 2026-09-10 01:19 | T216（`EquityCurve`/`EquityRecorder` + `Trade`/`TradeTracker` + `PerformanceMetrics`/`PerformanceAnalyzer`） | 通过：新增 38 个单测（alpha-backtest 48→86，全仓 244 全绿）。**净值采样点**：给 feeder 加了一个 `RoundListener` 缝隙（旧三参构造委托给新四参，行为不变），回调严格放在 `awaitRound` **之后**——「本根 signal→risk→order→fill 连锁已闭环」这件事**无法用 handler 注册顺序表达**，靠「注册在最后」实现的采样器会在有人多注册一个 handler 的那天静默变错，而在这里它是构造上精确的、且零成本。变异体「把回调挪到 `awaitRound` 之前」被两条用例杀掉：卡死的轮不得被报为已闭环（否则采样器会把半轮记账当成那根的结果），以及 `kline→signal→round-closed` 的逐元素交错序。`EquityRecorder` 对**同一业务时刻的第二次采样是替换而非追加**：两个序列同刻收盘会产生两轮同一个时间戳，而「该时刻的权益」意思是「该时刻所有事情都处理完之后」；追加会往收益序列里插一个零长度周期，把所有波动率指标压小。**交易归集**：一次 flat→flat 的往返是一笔交易而不是一笔成交一次（否则每个剥头皮腿都被叫做「交易」，40% 胜率看起来像 100%）；`Portfolio` 每标的只有一个净头寸，故每标的至多一笔未平交易，归集无歧义；翻仓的那一笔成交拆成平仓腿+开仓腿，**手续费按数量比例分摊且开仓腿的手续费用减法导出**（不是再分摊一次），两半严格加起来等于实收。交易盈亏**由现金流算出**（开仓付出、平仓收到、未平部分按标记价），而不是去读账本的已实现数：往返归零时两者构造上相等，这样均价算术只留在 `Portfolio` 一处；`theTradesAccountForExactlyWhatTheBookShows` 用 7 笔成交（含加仓/部分平仓/翻仓、双标的）钉死三条恒等式——`Σgross == realized+unrealized`、`Σfees == feeTotal`、`Σnet == equity-startingEquity`。**未平交易照报但排除在胜率/盈亏比之外**：把未实现结果计入会让这些数字取决于数据停在哪里。**「未定义」与「零」严格分开**：金额类结果是 Money scale 的精确 BigDecimal（重跑逐位一致，CI 阈值可用 `isEqualByComparingTo`，SC-02）；统计类是 `OptionalDouble`/`Optional<BigDecimal>` 而**不是 NaN 也不是 0**——分母为空时那个值是未定义，报 0 会被读成「没有风险」「没有亏损」，且能静默通过阈值断言。**年化只是照报，绝不用作阈值**：短窗口年化会剧烈外推（6 小时 +4% 复合成 7.39e24），有用例专门钉住这个量级，类注释指明 CI 该断言 `totalReturn` 与 `maxDrawdown`；年化按日历时间（含空洞）而夏普按采样频率年化，两者刻意用不同的时间观念。**写测试时查出两处真实缺陷**：①`best`/`worst` 原先播种在 0，全亏损的回测会报 bestTrade=0.00，读起来像「有一笔保本」——改为播种于第一笔已平交易，仅在从未平仓时回落到 0；②`payoffRatio` 原先只在 `averageLoss.signum()==0` 时短路，于是会去除 `averageWin` 那个**约定俗成的 0**，报出 payoff=0.00（读作「赢利单尺寸为零」而不是「没有赢利单」）——改为按 `wins==0 || losses==0` 的**计数**守卫。**删掉两处不可达守卫**而不是留着当杀不死的变异体：`worstDrawdown` 里的 `peak.signum()<=0`（`EquityCurve` 已校验 startingEquity>0，peak 只会被更大的样本替换，故恒正；爆仓是 ≥100% 回撤而非除零）与 `annualizedReturn` 里的 `yearsElapsed<=0`（周期已校验为正、曲线时间严格升序，跨度恒正）。公式一律用**独立参考实现**取值，不回读自己的输出：`/tmp/metrics_decimal.py`（Python `decimal`，MathContext(24,HALF_UP) 后 quantize 到 1e-8）出精确金额，`/tmp/metrics_ref.py`（IEEE double）出统计量；`Math.pow` 与 Python `**` 的差用 1e-9 容差吸收，其余 1e-12。**变异测试：18 个变异体全部杀掉**（空曲线拒绝、非正周期拒绝、爆仓仍出夏普、全亏仍年化、平直曲线 0/0、单点夏普、payoff 除以回落零、profitFactor 除以零 grossLoss、best 播种零、曲线接受非升序/零基线、同刻采样追加而非替换、翻仓判定反转、整笔手续费记在平仓腿、非正数量接受、开平腿只看 side 不看方向、平仓后仍留在 open 表、轮未闭环就报已闭环）。**另记一处 Maven 陷阱**：变异脚本用 `shutil.copy2` 还原源文件会把**原 mtime 一起还原**，比 `target/classes` 旧，Maven 的新旧判定因此跳过重编译，下一次构建跑的还是变异体的 class——还原必须用 `write_text`（盖新 mtime）；这个坑先让「还原后基线」假红了一次。 |
| 2026-09-10 01:41 | T217（`BacktestReport` + `ReportFormat` + `EquityCurveSvg` + `HtmlReportRenderer` + `ConsoleSummary`） | 通过：新增 28 个单测（alpha-backtest 86→114，全仓 244→272 全绿）。口径与 spec 澄清一致（spec.md:268「HTML 静态报告 + 控制台摘要」），FR-BT-04 七项指标 + 逐笔成交明细齐备，边界 3 的缺口取自 `ReplaySummary.gaps()`。**自包含**：用例禁掉 11 种引用形式（`<script`/`<link`/`@import`/`src=`/`href=`/`url(`/`http:`/`https:`/`<iframe`/`<img`/`<object`/`<embed`）——CDN 依赖今天照样渲染得好好的，等网络或那个库没了才炸，是典型的静默失败；内联 SVG **刻意不写 `xmlns`**（HTML5 内合法，且这样整份文档里连一个 `http:` 字符串都不存在），变异体 H2 把 xmlns 加回去立刻被这条抓住。**确定性（SC-02）**：`BacktestReport` 里唯一带墙上时钟的值是 `ReplaySummary.elapsed()`，**刻意不进 HTML**——三次逐位比对里它每次都不同，而它看起来完全正常，正是最难发现的那类破绽；它只进控制台摘要，因为 SC-01 是计时验收、那个数字必须能从这次运行本身读出来，而控制台文本没有任何东西逐位比对，所以安全（两个类的注释各写了一遍理由）。`Locale.ROOT` + `ZoneOffset.UTC` 全覆盖：用例把 JVM 默认 locale/时区翻成 de-DE、ar-SA、fr-FR 与 Pacific/Kiritimati vs Pacific/Pago_Pago，再与**原始默认下捕获的基线**比对，HTML 与控制台各比一次（H8/H9 证明会咬人）。**转义是「单点发射」规则**：只有 `fact`/`metricRow`/`table` 三处让文本进 markup、各转义恰好一次，所以所有调用方一律传原始文本。初版的行 lambda 里也调了 escape，于是 `&` 渲染成 `&amp;amp;`；清洗到恰好 10 个发射点，并用 `doesNotContain("&amp;lt;")` 钉住「不多转一次」。`metricRow` 的转义**当前没有任何测试能区分**（走它的全是格式化数字或字面标签），仍然保留：删掉它会把一条统一规则变成一个例外，而第一个把 symbol 名字塞进指标行的调用方会静默继承这个洞——它就是**唯一存活的变异体 H10**，作为「构造上等价」写进类注释，不假装有测试钉住。**降采样**：三年小时线 ~26k 点，`MAX_POINTS=2000` 按 stride 抽，但**最后一根永不跳过**（整条曲线就是拿它来读的）。证明方式刻意不复制几何：构造一条 5000 点曲线与一条 2 点曲线，两者 low/high/起始权益/首末时间戳全同，于是共用一个 scale 与一条 x 轴，断言**末顶点逐位相同**（H4「末根被 stride 跳过」与 H12「从不降采样」被同一条用例的不同断言分别杀掉）。单点曲线画 `<circle class="dot">` 而不是 polyline——一个顶点的 polyline 什么都不渲染，读起来像「没有数据」。**y 轴刻度自适应**（写完初版自查发现的真实弱点）：原先固定 0 位小数，小资金或短而安静的区间会让五个刻度全部塌到同一个整数，坐标轴等于什么都没说；`labelScale(span)` 按量级给 6/4/2/0 位，并**在方法注释里写明这是全项目唯一允许取整的地方**——它们是视觉辅助线而不是报告值，故与 `ReportFormat` 的「绝不为显示取整」不矛盾，这句话是防止后来者把它当成可推广的例外。**金额显示绝不取整**：`stripTrailingZeros().toPlainString()`，scale 8 的零打成 `0` 而不是 `0E-8`。写的时候先加了个 `signum()==0` 特例，随后**用一次性 Java 程序实测**确认 JDK 21 上 `new BigDecimal("0.00000000").stripTrailingZeros().toPlainString()` 本来就是 `"0"`（JDK-6480539，Java 8 已修），于是**把这段死代码删掉**而不是留着当杀不死的变异体，并把用例注释改成指向那个 JDK 号。**未决事项一律浮到表面**：`hasUnresolved()` = 未成交挂单 / 拒单 / 未平仓 / 未平交易 / 数据缺口，任一非空就在报告顶部出横幅，控制台摘要列**同一份** `warnings(report)`（一处定义，两边不可能漂移）；空段落显式写「无」而不是只留一个光秃秃的标题——T215 记下的「最后一根的信号没等到成交」这条事实，就是靠这里才不会被埋掉。**表格列数装了绊线**：`headers.size() != values.size()` 直接抛 `IllegalStateException`，因为列错位会安静地把「手续费」印在「数量」底下，而 HTML 照样打得开、照样好看。**变异测试：21 个变异体杀掉 20 个**（表格不转义、xmlns 外链、耗时进文件、末根被跳过、未定义渲染成零、空段落无声、单点画隐形 polyline、格式化跟随机器 locale、时间戳跟随机器时区、回撤不标在曲线上、从不降采样、charset 不声明、控制台藏起未决项、金额按两位取整、双引号不转义、百分比走二进制浮点、刻度恒为整数、刻度恒为 6 位、亚单位区间不多给小数、整数刻度阈值放宽）。 |
| 2026-09-10 01:57 | T218（`BacktestRunner`：纯 Java 装配全链路，占位类整体替换） | 通过：新增 10 个单测（alpha-backtest 114→124，全仓 272→282 全绿），零 Spring。**装配的三条不可见规则写进类注释**（代码本身看不出来）：①`SimulatedExecutor` 注册在 `StrategyEngine` 之前——两者都吃 `KlineEvent`，注册序即分发序；反了**不是**未来函数泄漏，而是策略会在交易所已经改过的持仓上做决策（同样错，只是错法不同）。②`EquityRecorder` 走 feeder 的 `RoundListener` 而**不是** handler，且它根本不实现 `EventHandler`，所以「误注册成 handler」这个错**类型系统直接不允许**——比靠注释守更硬。③除 executor 与 `StrategyEngine` 的 mark 之外**没有任何组件写 Portfolio**，再加一个记账 handler 就会把每笔成交记两次。**风控拦截刻意不进报告**：`RiskGate` 已按 WARN 逐条打了 ruleId，runner 只把 `ordersPassed/signalsBlocked` 汇总进运行日志；塞进 `BacktestReport` 等于给读者同一份信息的第二个、过滤口径还不一样的视图。**夹具是照着「一次装配错误必须被发现」设计的**：bar0 open=100 / close=101、bar1 open=102——**开盘价刻意不等于上一根收盘价**，否则「次根开盘成交」与「信号根收盘成交」在数字上无法区分。于是把注册顺序反过来会**同时**改变三个可观测量：成交根变成 bar0、滑点从 20bp 掉回固定的 5bp（订单在撮合器还没见到 bar0 时就挂了，`slipOfLastBar` 里查不到它，落回 `getOrDefault`）、成交价从 102.21 变成 100.05；变异体 R1 被这三条一起杀掉。**手算黄金值直接钉住整条链**：qty `29.702`（0.30 × 10000 权益 / 101 标记价，按 stepSize 0.001 向下取整）、滑点 `20bp`（5 固定 + bar0 的 300bp 振幅 × 0.05）、成交价 `102.21`（102 × 1.0020 按 tickSize 0.01 对交易者不利地 CEILING）——这三个数一次跑通即证明 sizer 拿到了正确的权益/强度/标记价、撮合器拿到了正确的下单根与成交根。**`samplingPeriod` 取「最细周期」并把代价写明**：一轮一根、一根一采样，故最细周期是「近似恒定间距」里最诚实的那个，单序列（P2 出货的每一种）就是它的 bar 周期；混合周期确实不等距、夏普年化处理因此是近似——**明说而不是藏起来**，因为另一种做法（让配置里的周期可以与真实回放的数据不一致）更糟：它会让数字看起来精确其实是错的。测试把 ETH H4 放在序列列表**第一位**，使「第一个序列的周期」与「最细周期」不同，R8/R9 两个变异体都被杀；这条测试顺带跑到了 T216 的「同刻采样替换」路径（13 轮 → 11 个点）。**两处「拒绝运行」而不是「跑出个没意义的结果」**：零策略（会产出平直净值曲线与「0 笔交易」的报告，和「策略刚好保本」无法区分，通常成因是把策略全关掉的配置笔误）与非正初始资金。后者特意**在 `Config` 里就拦**：0 权益会让 sizer 一单不发、白跑 10 根、最后在 `EquityCurve` 那里报一条**关于曲线**的错；用例连消息文本一起钉住（`startingEquity must be positive`），否则 `EquityCurve` 那条同样含 "startingEquity" 的错会让变异体 R13 蒙混过关。**`engine.stop()` 放 finally**：引擎循环线程不是守护线程，一次抛异常却没停引擎的运行会把 JVM（或 surefire fork）挂在失败**之后**；用例数 `event-engine` 线程数前后一致，变异体 R3（去掉 finally）被它杀掉，且并没有把 surefire 挂死（3.x 的 `forkedProcessExitTimeoutInSeconds` 会强杀 fork 并报错）。**一个 `Config` 只能跑一次**（写在 `run()` 注释里）：`Strategy` 跨 bar 带状态（指标窗口、金叉是否已发过），复用同一份 Config 跑第二次等于往已预热的策略里再灌一遍数据；SC-02 的三次复现必须**重建策略**而不是重跑同一个 runner——**T220 照此写**。**未决事项贯通**：最后一根的信号永远等不到成交，`pendingOrders()` 必须进报告（R4 被杀），funding（R5）、openPositions（R6）、series（R7）同理。**数据源可替换是实测的而不是声称的**：同一份 10 根 bar 分别经内存仓储与 `CsvKlineRepository`（`@TempDir`）跑，两份 HTML **逐字节相同**——FR-BT-06 的「换存储不得触及 feeder/策略/撮合」由此有了正面证据。**变异测试：13 个变异体全部杀掉**（注册顺序反转、净值采样器不接、失败时不停引擎、挂单/资金费/持仓/序列各自从报告里丢掉、采样周期取第一个与取最粗、TradeTracker 未注册、RiskGate 未注册、两处守卫删除）。 |
| 2026-09-10 02:24 | T219（alpha-app backtest 装配：`AlphaProperties.Backtest` + 新增 `BacktestWiring` + 三处 profile 收窄 + `application-backtest.yml`） | 通过：新增 22 个单测（app 10→31、backtest 124→125，全仓 282→304 全绿），并**手工跑通打包产物**：`java -jar target/alpha-app-0.1.0-SNAPSHOT.jar --spring.profiles.active=backtest …` → **EXIT CODE 0**、墙上时间 ~1.0s、`backtest-report.html`（7839 字节）落盘、控制台摘要 60 根 / 1 笔成交 qty=36.14450 / 权益 10000→10605.0517011 / 最大回撤 0% / 夏普 58.236 / **年化 530622.3150%** / 手续费 1.5036112 / 资金费 0.6722877 / elapsed 12ms / 1 项未决。qty 的 5 位小数顺带证明 yml 里嵌套的 `rules` 列表真的绑定上了（stepSize 0.0001）；那个荒谬的年化数字则是 T216「年化只照报、绝不当阈值」的实物证据 —— **T220 的阈值断言只用 `totalReturn` 与 `maxDrawdown`**。**「跑完自动退出」不是配置出来的，是把在线装配从 backtest profile 里拿掉之后才成立的**：`StartupWiring` 原先是无条件 `@Component @Order(1) ApplicationRunner`、`EngineConfig` 的三个 bean 也无条件，于是 backtest 下照样 new 出 `EventEngine` 并 start，而**它的循环线程刻意不是守护线程**；Spring Boot 非 web 应用在 `main` 返回后只在「只剩守护线程」时退出，所以进程永远不终止（这也是验收时最先撞上的现象：报告已经写完、进程还挂着）。修法是把 `EngineConfig`/`StrategyConfig.strategyEngine`/`StartupWiring` 收窄到 `@Profile({"paper","live"})`、`BacktestWiring` 反向收窄到 `@Profile("backtest")`；顺手**删掉**两处因此变成死代码的东西 —— `EngineConfig` 里没人用的 `virtualClock` bean，以及 `StartupWiring` 里恒不成立的「本 profile 没有 gateway」分支连同它那条过期的「T214 会有 feeder」日志。**`StartupWiring` 同时改为直接注入 `ExchangeGateway`**：`GatewayWiringConfig` 本来就是 `@Profile({"paper","live"})`，在这两个 profile 里那个 bean 必然存在，`ObjectProvider` 的兜底分支不可达 —— 按纪律删掉而不是留着当杀不死的变异体。**变异体 E1/S1/U1（拆掉三处 profile 限制）只有能启动容器的测试看得见**，这正是 `BacktestProfileApplicationTest` 存在的理由：`@SpringBootTest` + `@ActiveProfiles("backtest")` 断言容器里**没有** `EventEngine`/`StrategyEngine`/`ExchangeGateway` 三个 bean、JVM 里**没有**名为 `event-engine` 的存活非守护线程、报告存在且含标的与初始资金、yml 那一块（含只有 yml 里才有的 `rules[0]`）真的绑定成功。**数据集在 `static {}` 里、容器加载之前写好** —— runner 是在启动过程中执行的。**配置面按原则划而不是按任务文字划**（取舍 6）：`alpha.backtest.*` 只放真正回测专属的旋钮，**初始资金与策略启停留在共用的 `alpha.initial-cash` / `alpha.strategies`**，于是「回测配的账本或策略集与实盘不同」在配置层面根本表达不出来 —— FR-BT-06 的同构由「同一批类」延伸到「同一份配置」。`exposure`/`cost`/`quiescenceTimeout` 三个旋钮**刻意保持可空**，为空时把领域里的那三个常量原样交给 runner，让数字只有一处定义。**缺区间是拒绝运行而不是猜默认值**（取舍 7），且**全部校验发生在碰文件系统之前**：一次因缺区间被拒的运行不该在退出路上留下数据库文件或 journal。**`Config` 新增第三条拒绝**（排在非正权益与零策略之后）：**被交易的标的必须有精度规则**。没有 tickSize/stepSize 时 `RiskGate` 会把每个信号都拦成 RK-07，于是这次运行回放完所有 bar、一单不发、报出一条平直曲线，读起来正是「这个策略保本」。**按策略交易的标的查而不是按回放的序列查** —— 多回放一条没人下单的上下文序列是合法的（T218 的采样周期用例就回放了 ETH 却从不交易它，所以那条用例必须继续通过）。守卫放在 `Config` 而不是 app 侧，于是每个调用方（包括 T220 的 CI 冒烟）都被保护到。**yml 里的精度规则是实测来的不是编的**：`tick-size 0.10 / step-size 0.0001 / min-notional 50` 取自 2026-09-09 对 `testnet.binancefuture.com/fapi/v1/exchangeInfo` 的真实抓取，注释里写了快照日期与「testnet 过滤器可能与生产不同，信 minNotional 行为前先复核」。**`source: db` 只接受 SQLite**，非 `jdbc:sqlite:` 的 URL 被拒并说明「本模块不带别的 JDBC 驱动」。**journal 打开时先 `deleteIfExists` 再建**：`JsonlEventJournal` 是**追加**写的，不删就把上一次的事件接在后面，而这份文件是拿来逐行比对的。**刻意不断言的东西写在测试注释里而不是悄悄跳过**：`quiescence-timeout` 没有用例 —— 它只在「某一轮没能闭环」时才起作用，1ns 超时是否咬人取决于哪个线程先赢，在这里测它就是一条假装成覆盖率的抖动测试，交给实现它的那个组件去测。**变异测试：26 个变异体杀掉 25 个**（缺区间被放行、未知 source 回落 csv、两处 URL 校验失效、db 静默走 csv、序列派生方向两向反转、敞口/成本/`orDefault` 三个旋钮被忽略、规则列表被清空、成本模型换成默认、from/to 互换、权益写死 10000、报告不写与写错文件名、journal 恒为 noop 与不删旧文件、三处配置默认值改动、三处 profile 限制拆除、`Config` 新守卫删除）。**唯一存活的 W19（`quiescence-timeout` 恒取默认值）与上面那条「刻意不断言」是同一个事实的两面**：不是漏测出来的侥幸，而是这个旋钮在单元层面不可观测，已在 `BacktestWiringTest` 的类注释里写明理由，不假装有测试钉住它。 |
| 2026-09-10 02:45 | T220（CI 回测冒烟：提交 2400 根真实数据集 + `BacktestSmokeTest` 4 个用例 + `BacktestSmokeFixtureTest` 按需再生成 + `PROVENANCE.md`） | 通过：新增 5 个测试，全仓 **304→309 全绿**（2 个跳过 = 两个按需联网测试：夹具再生成与 testnet 下载）。**SC-02 有了实物证据**：同一份输入连跑三次（每次 **重建** `BacktestWiring`，因为 `Strategy` 跨 bar 带状态，重跑同一个 runner 是往预热过的策略里再灌一遍数据），`metrics()`/`curve()`/`trades()`/`fills()`/`funding()` 五个记录全等 **且 HTML 逐字节相同**，surefire 计时 **0.423s**（三次 2400 根回放的总和）—— 这也是 SC-01 的早期信号：26k 根按此外推约 4.6s 回放。**数据集是真实抓来的、不是编的**：走真正的 `BinanceKlineDownloader → CsvKlineRepository` 路径从 `testnet.binancefuture.com` 拉 2024-01-01T00:00:00Z 至 2024-04-09T23:00:00Z 的 BTCUSDT 1h，**2 次请求（真的翻页了）**、`skippedForming=0`，落盘 **174917 字节**、sha256 `992672de066e55114cf8112681c88174a1b4264425cf2852f4dc020844ec61a5`；再生成测试顺带证明幂等（**第二次同区间下载 stored=0**）。一个 testnet 的细节记在 `PROVENANCE.md` 里：**它拒绝 `BTCUSDT.PERP` 这个符号**，抓取时按 `BTCUSDT` 请求、落盘时按统一符号命名，否则再生成会静默产出空目录。**钉住的黄金值（实测得来，第一稿里那些编的数全被换掉了）**：`totalReturn -0.13417588`、`maxDrawdown 0.15788502`、`finalEquity 8658.24122746`、`totalFees 338.75374834`、`fundingTotal 10.59291420`、`fills 123`、`closedTrades 122`、`openTrades 1`、`wins 32`、`losses 90`。**基线是亏损的，这件事写在类注释里而不是藏起来**：绿灯说的是「管道没变」，**完全不代表 MA 双均线值得跑**（100 天小时线、扣 taker 费+滑点+资金费后 26% 胜率，正是一个趋势跟踪策略在震荡窗口里的样子）——策略好坏是读报告的人的判断，不是 CI 的。**年化按取舍 8 只报不断**：实测 `-0.4089580146693411`；`winRate 0.26229508196721313` 与 `sharpe -1.7422310888491224` 打印但也不单独钉 —— 前者是 `wins/closedTrades`（两个都已钉），后者由净值曲线推出（曲线在确定性用例里已逐位比对），再钉一遍只是给同一批输入多两个必须同步修改的字符串。**数据集自检用一条网格断言**：`bars.get(i).openTime() == FROM + i*HOUR` 对 2400 个 i 成立，一句话同时蕴含了条数、顺序、端点与**无空洞**；另外单列 `gaps()` 为空，因为 committed 数据集里出现一个洞不会响亮地失败，只会让每个黄金值移动一个看起来像策略 bug 的量。**变异测试 14 个，杀掉 13 个**，且刻意瞄准**链路**而不是装配（装配已由 T219 的 26 个覆盖）：mark 价改用开盘、成交价改用收盘（未来函数）、CEILING/FLOOR 互换、手续费按开盘价算、taker 费率 0.0005→0.00045、回撤峰值不更新、总收益漏减 1、金叉守卫删除、信号强度 1.0→0.5、目标敞口 0.30→0.31、CSV 解析改走 double、CSV 排序反转、以及**唯一一个只为 SC-02 而设的变异体**（HTML 里多打一行 `Instant.now()`/`System.nanoTime()` —— 它被三次逐字节比对杀掉，证明那条断言真的在看守「墙上时间不得进被比对的产物」）。**唯一存活的 M3（把 `slipOfLastBar.put` 移到 `fillPending` 之前）经查是等价变异体，不是漏测**：该 map 的唯一读点在 `onOrderRequest`（存进 `Pending` 后 `fill` 只读 `Pending.slippageBps()`，`fillPending` 从不碰 map），而 `EventEngine.publish` 是 `externalQueue.offer(event)` —— **入队，不是重入分发**，所以 `onKline` 执行期间没有任何订单能被下。这个重入问题是值得查的（`Strategy.onFill` 确实存在且确实能发单），但从 `onFill` 发出的 `OrderRequestEvent` 同样排在 `onKline` 之后才被消费，因此 put 相对 `fillPending` 的位置**对任何策略都不可观测**。**推论是原注释说的是错的理由**（「Recorded after the fill: 后下的单落进这根 bar…」），已改写为真正的不变量：*在本 handler 返回之前记录，这才是「用这根 bar 的振幅」成立的原因*。**夹具解析走 classloader（`getResource().toURI()`）而不是 `src/test/resources/...`**：冒烟测试是唯一一个不许依赖工作目录的测试，在某种 IDE 运行配置下报「数据集找不到」会教会人去忽略冒烟测试。 |
| 2026-09-10 03:12 | T223（补：历史数据下载入口 `download` profile + `AlphaProperties.Download` + 抽出 `DataPlan` + `application-download.yml`） | 通过：新增 22 个单测（app 36→58，全仓 309→**331** 全绿，2 个跳过 = 两条按需联网测试），并**手工跑通打包产物**：`java -jar … --spring.profiles.active=download --alpha.download.from=2023-09-10T00:00:00Z --alpha.download.to=2026-09-09T00:00:00Z` → **EXIT 0**、墙上 8s、**18 次请求**（真的翻页了）、**26281 根 fetched/stored、0 根因未收盘被丢**，落盘 `data/klines/BTCUSDT.PERP-1h.csv`（1934662 字节，sha256 `b4484e26…b94584`，gitignored 不提交）；**紧接着同区间重跑 stored=0** —— 幂等，这也是「我的库是不是最新」这个问题唯一诚实的答法。**补这条任务的原因是产品缺口而不是任务缺口**：FR-BT-05 要的是一个*工具*，而 T212 的 `BinanceKlineDownloader` 只有 JUnit 能调到，`application-backtest.yml` 却把 `csv-dir` 注释成「下载器写进来的目录」—— 没有任何东西往里写，SC-01 也只有写过一次性 harness 的人能复现。**验收时查出一个真实隐患（已修）**：`alpha.backtest.data` 原先只声明在 `application-backtest.yml`，而 **Spring 只为激活的 profile 加载 `application-<profile>.yml`**，download profile 根本不读那个文件 —— 它落到 `AlphaProperties.Backtest.Data` 的 record 默认值 `data/klines`，**与回测一致纯属出厂值恰好相同**。操作员一旦把 `csv-dir` 改成别的路径，就会得到「下载成功、回测说没数据」，正是取舍 9 声称不可能发生的那个现象。修法是把 `data` 块搬进**共享的 `application.yml`**（取舍 6 的同一原则：它不是回测专属旋钮，是两个模式共用的存储），并加 `ShippedConfigurationTest`（3 个用例，**不起容器**，用 `YamlPropertySourceLoader` 直接问「哪个文件声明了什么」）。**断言的是「已声明」而不是「绑定后的值」**：record 默认值与出厂值同为 `data/klines`，绑定结果在「文件里写了」与「回落默认」两种情况下不可区分，所以必须问 property source 本身；另两个用例分别断言两个 profile 文件**各自单独加载时都查不到这个 key**（与共享文件一起加载时，重复声明同一个目录是不可见的），以及两个 profile 解析出同一个 store。变异体 D21（从 `application.yml` 删掉 data 块）与 D22（塞回 `application-backtest.yml` 且改成 `/mnt/klines`）都被杀掉。**离线验收用 JDK 自带的 `com.sun.net.httpserver.HttpServer` 做 stub**（`mockwebserver` 不在本地 m2 而构建是离线的）：它记录每个查询、**只服务落在请求窗口内的 bar**，于是「区间被换掉或截断」表现为 bar 变少而不是被静默接受；需要失败时用 `failWith(400)` 而不是关掉的 socket —— 连接错误会被下载器按 5 次指数退避重试，一条断言会变成 15 秒。**`settings()` 与 `release()` 都做成 package-private static**，理由写在 Javadoc：否则「testnet/live 映射」与「释放到底做了什么」都只有联网才测得到。**容器测试断言 download profile 下没有 `EventEngine`/`StrategyEngine`/`ExchangeGateway`/`BacktestWiring` 四个 bean、恰好一个 `DownloadWiring`、JVM 里没有名为 `event-engine` 的存活非守护线程** —— 与 T219 同一套结构性论证（承重墙是 profile 限制，拆掉它只有能启动容器的测试看得见）。**变异测试 22 个杀掉 21 个**：三处拒绝、testnet/live 两向、空 base-url 当成有值、override 顺手重置退避预算、序列方向反转 / 全局周期被忽略 / 显式列表变成追加、写到别的目录、区间互换、db→csv 回落、未知 source 回落、SQLite 校验删除、`@Profile("download")` 拆除、`symbol.binance()`→`unified()`、连接池不 evict、dispatcher 不 shutdown，加上面两个 yml 变异体。**唯一存活的 D11（`download()` 不调用 `release()`）经查是不可观测，不是漏测**，判定**靠实测而不是推断**：先加一条直接钉 `release()` 的用例（发一次真请求 → 断言连接池非空且 executor 未 shutdown → 调 `release` → 断言池空且 `isShutdown()`），D12/D13 因此被杀；再写一次性探针采样 10×100ms，发现 **`OkHttp TaskRunner` 是守护线程且是 JVM 级单例，释放之后照样 TIMED_WAITING 活着**，released 与 unreleased 的线程集合完全相同，唯一差别是池里那条连接（unreleased `pool=1`），而那个 client 在 `download()` 里创建并丢弃、外部拿不到。把 client 注入进来只为可测性，等于把 HTTP client 塞进装配的公开契约，不划算 —— 结论写进 `release()` 的 Javadoc（「删掉这个 finally 之前先知道它挡的是什么：工具已经打印 complete 之后还攥着一条 socket 和一个清理任务」），不假装有测试钉住。**变异脚本的新陷阱**：D11 第一版的替换文本调用了一个不存在的方法，而**编译失败的变异体不算被杀**（构建失败会被记成 KILLED，理由却是错的），已改成编译通过的 `if (series.isEmpty())` 守卫 —— 写变异体时先问一句「这个改动能编译吗」。 |
| 2026-09-10 03:18 | T221（SC-01 性能验收：BTCUSDT 1h 近三年完整回测 + 报告） | **达标，余量是两个数量级**。数据集 = 用 T223 的 download profile 从 testnet 拉的 **26281 根**（2023-09-10T00:00:00Z → 2026-09-09T00:00:00Z，1095 天 × 24 + 1，sha256 `b4484e26…b94584`，1.9MB，放 gitignored 的 `data/klines`、**不提交**）。跑法就是操作员的跑法（`mvn -B -o package -DskipTests` 之后两条 `java -jar`），**实测：回放 425ms（feeder 自报 `PT0.425222083S`）、整个 JVM 墙上 1.379s（含 Spring 启动 0.575s、CSV 解析、958962 字节 HTML 落盘）、EXIT 0**，而预算是 **5 分钟**（≈218 倍余量）。`Backtest done: bars=26281, orders passed=1154, filled=1154, rejected=0, still pending=0, equity samples=26281`；**0 处缺口** —— testnet 三年小时线是完整的，所以这份计时既不含缺口处理的开销，也不需要它。**顺手把 SC-02 在 26k 根上又验了一遍，而且比 T220 更强**：三个**独立 JVM 进程**（默认、`report-dir` 换到 /tmp、`journal=true`）产出的 HTML **sha256 完全相同**（`f6946261…1ec52d`）。T220 钉的是同一进程内三次运行逐位一致，这里证明的是**跨进程、跨 journal 开关**也一致，即墙上时间与 journal 都没进被比对的产物。**测出一条与注释不符的事实并改了注释**：`application-backtest.yml` 原先说 journal 默认关是因为「三年小时线每根数个事件，写它们会与 SC-01 的 5 分钟竞争」；实测 `journal=true` 是 **746ms 回放 / 1.758s 墙上 / `events.jsonl` 8274642 字节**，即只 **+0.3s**，对 5 分钟毫无威胁 —— 真正的代价是那次运行多出一个 8.3MB 的产物。注释已换成实测数字与真正的理由（按 T220 的 M3 先例：**测量与注释冲突时，按测量改注释**）。**刻意不加 CI 计时断言**（沿用 T220 的判断）：一条「< 5 分钟」的断言在慢 CI 上只会变成抖动源，而 SC-01 的验收标准本来就是「实测计时记录在执行日志」；**可复现性由 T223 保证**（两条命令 + 数据集 sha256），不靠把 1.9MB 塞进仓库。**结果本身照旧不是策略评价**：权益 10000 → 2007.81680474（**-79.92%**）、最大回撤 81.25%、夏普 -1.947、年化 -41.4424%、1153 笔已平 + 1 笔未平、胜率 26.71%、盈亏比 0.5636、手续费 1741.54443593、资金费 39.87562933 —— MA(10,30) 在三年小时线上扣掉 taker 费 + 滑点 + 资金费后是稳定亏损的，与 T220 那条 100 天亏损基线同方向、只是窗口更长。SC-01 说的是「跑得完」，不是「值得跑」。 |
| 2026-09-10 03:25 | P3 细化 | 生成 **T301-T321**（21 条，其中 T321 阻塞）。细化时查出并解决了 5 处上游文档之间/与既有实现之间的矛盾，全部记进取舍 11-18 而不是就地拍板：①DESIGN §8 的「平面管道」装不下订单级/组合级规则（它们要数量，数量是 RK-07 换算出来的）→ 两阶段管道；②DESIGN 的 `marginRatio≥150%` 与 Binance 同名指标**方向相反**，直接对接会让快爆仓的单通过检查 → 规则自算，交易所那个字段只进对账；③DESIGN 说规则放 `risk-rules.yml`，但取舍 9 已证明「profile 专属文件 + 回测实盘必须同值」是陷阱 → 放共享 `application.yml`；④plan §2 把频率级划给 P4，而 P3-2 的验收 SC-04 要求「全部五级」→ 按 §6+SC-04 在 P3 做；⑤DESIGN §11 的四张表放不下 FR-RK-08 的拦截记录与持仓历史 → 扩成六张。另查出**一处 P2 遗留的默认值矛盾**（取舍 17）：`targetExposure=0.30` 配上 DESIGN 的「单笔 ≤20% 权益」会拦掉所有翻仓与所有建仓到 30% 的单 —— P2 没有订单级规则所以从未暴露；耦合式 `单笔上限 ≥ 2 × targetExposure` 写进 yml 注释，出厂敞口降到 0.10。阻塞项：T321（场景 2 testnet 全链路）与 T316-T318 的实跑部分需要 API Key，验收改为离线桩 + 真实报文样本单测，实跑留作用户验收项（同 T112）。 |
| 2026-09-10 03:40 | T301（`RiskRule` SPI + `SignalFacts`/`OrderFacts` + `RiskPipeline` + `RiskGate` 改为管道宿主） | 通过：新增 20 个单测（`RiskPipelineTest` 15 + `RiskGateTest` 9→14，alpha-risk 26→46，全仓 331→**351** 全绿，2 个跳过 = 两条按需联网测试）；`BacktestSmokeTest` 四个用例照旧通过 —— **出厂装配仍然传 `RiskPipeline.empty()`，所以钉住的黄金值不是「碰巧没变」而是构造上不可能变**，真正换上五级规则是 T302/T308 的事，届时数字必然移动并按 `PROVENANCE.md` 重新实测。**原计划里的单一 `RiskContext` 拆成了两个记录**：一个上下文类型必须给「数量」留一个可空的槽位，于是「订单级规则读到了一个还没被换算出来的数量」这件事在类型上是可表达的，只能靠注释和纪律守；拆成 `SignalFacts`（信号 + 账户快照）与 `OrderFacts`（候选订单 + **成交后的账本投影**）之后，PRE_SIZE 阶段的规则**拿不到**数量，编译期就成立。`OrderFacts` 的投影是这一层的全部价值：查当前总名义会放过「一步一步把已超限的账本继续做大」的每一单，只查本单名义会漏掉「相对权益很小但把单一标的翻倍」的单。**投影按「换掉本标的在总名义里的那一片」算**（`total - symbolNotional + projectedSymbolNotional`）而不是重新对全账本 mark 一次：其他持仓没动，重读会让投影依赖于两次读之间到达的标记价 —— 与 `RiskGate` 的**单快照规则**同一条理由，整条决策链只 `SignalFacts.of` 一次，规则、sizer、拦截记录看到的是同一个对象（用例断 `isSameAs`，不是 `isEqualTo`）。**`RiskGate` 的四步顺序**：缺精度规则 → 信号级规则 → FR-RK-07 换算 → 订单级规则。缺规则排在最前，因为没有 tickSize/stepSize 时任何规则算出来的名义价值都是编的；信号级排在换算之前，用例用**强度 0**（换算结果是「无需交易」，本该什么都不发）来证明熔断规则确实先跑过 —— 换算先行会让这次尝试静默结束，规则永远不知道自己被问过。**两条构造期守卫**：①跨两阶段的 ruleId 去重（告警与拦截记录都只按 id 索引，同号两阶段会让「哪条真的响了」变成歧义）；②`attributed()` 校验规则返回的 rejection 必须写自己的 id **和** level —— 少了它，一条复制粘贴出来的规则会把单子正确拦下、告警读起来也合理，而拦截记录指向一条从未响过的规则，这类错只有凌晨三点试图给自己解释「这单为什么被拦」的人才会发现。**变异测试：14 个变异体全部杀掉**（去重守卫关闭、两阶段各自绕过归因校验、归因校验的 id 半条与 level 半条分别删除、信号级短路改成取最后一条、投影忘记减掉旧片、BUY 投影变成减、事实交出绝对数量而非带符号数量、投影名义忘记取绝对值、订单名义改用投影数量、`RiskGate` 跳过信号级、跳过订单级、订单级重读账本）。**跑变异查出测试自身的三处缺陷，都改了测试而不是放过变异体**：①`theTwoStagesAreIndependent` 里一条 `assertThat(signal.seen).isEmpty()` 永远不可能通过（前面那次 `checkSignal` 已经记了一条），改成「各记一条、谁也没伸进对面的列表」；②归因用例原先让 id 与 level **同时**不同，于是删掉校验的任意一半都照样抛 —— 拆成「同 level 错 id」与「同 id 错 level」两条，两个比较才各自承重（这正是 D5 第一版存活的原因）；③长仓用例里 `Position.qty()` 与 `signedQty()` 数值相同，**「事实交出绝对数量」这个变异体在只有多头的测试下不可检测**，补了一条空头账本用例（-10 的带符号数量、1000 的名义敞口、买 30 覆盖后是 +20 而不是 40）—— 空头是唯一让两者分叉的地方，也是符号 bug 唯一的藏身处。**脚本陷阱复现**：D7/D8 第一版替换文本的换行位置与源码不符（`PATTERN x0`），按纪律不算杀也不算存活，改正后重跑。 |
| 2026-09-10 04:15 | T302（`AlphaProperties.Risk`：五层参数 + 每层 `enabled` 开关，声明在共享 `application.yml`） | 通过：`mvn -B -o clean verify` 全绿（alpha-app 73 个用例，含新增 `RiskPropertiesTest` 12 + `ShippedConfigurationTest` 3→6，全仓 2 跳过不变）。**新增 `Risk` 记录树**（account/order/portfolio/breaker/frequency/sizing），每层一个 `Boolean enabled`——用装箱 `Boolean` 并在 null 时归一到 `TRUE`，因为原生 `boolean` 会把「没写这个键」绑成 false，从而**静默关掉一整层**，而关掉的风控不会报错、只会在出事那天缺席。**五层的数值同时从两侧钉到 DESIGN §8**：`RiskPropertiesTest` 把出厂 `application.yml` 绑回来逐条比对（3x / 1.50 / 0.20 / 0.02 / 0.60 / 0.30 / 0.05 / 3 笔 / 2h / 10 单 / 1m），并断言 `Risk.DEFAULTS` 持有**同一批字面量**——两侧各自与 §8 的常量比，而不是互相比（互相比会在两者一起漂走时照样绿）。改 yml 的人看到绑定侧偏离 §8，改代码默认值的人看到 DEFAULTS 侧偏离 yml，只有两边一起改才能保持绿，这正是「这些是账户的限额、不是自由参数」该有的摩擦力。**敞口旋钮从 `alpha.backtest.exposure` 移进共享 `alpha.risk.sizing`**：一个信号能吃掉多少账户是风控参数，回测/模拟/实盘必须同样换算，否则回测不再是关于实盘的证据（FR-BT-06）；profile 专属的敞口会让三种模式各说各话。**`target-exposure` 在 yml 里刻意不声明**（只留注释）：数值的唯一定义在 `PositionSizer.Policy.DEFAULT`，再写一遍就是第二个会漂的定义；而且一个只有注释子键的 `sizing:` 空节点**根本绑不上**——Spring 会把空标量往 `Sizing` 记录上转，报 `ConverterNotFoundException`，这条是本次 clean 重建时才炸出来的。缺块由 `Risk` 归一到 `Sizing.DEFAULTS`（`effectiveTargetExposure()` 回落到 sizer 默认）。**取舍 17 的耦合式落成了启动期拒绝**：`Risk` 紧凑构造器要求 `order.max-notional-fraction ≥ 2 × targetExposure`，因为翻仓是「平多 + 开空」共 2×敞口的**一笔**单，上限比这紧就会拦掉每一次翻仓和每一次建仓到目标，而账本继续持有策略早已反向的仓位，唯一的症状是一串读起来像规则 bug 的告警。这条不等式**逼着** `PositionSizer.Policy.DEFAULT` 从 0.30 降到 0.10（0.30 的翻仓单 = 0.60 > 0.20 上限）。**黄金值因此移动，全部按 `PROVENANCE.md` 重新实测而非估算**：`BacktestSmokeTest` 五个金额值 totalReturn -0.13417588→**-0.04561396**、maxDrawdown 0.15788502→**0.05452565**、finalEquity 8658.24122746→**9543.86042895**、fees 338.75374834→**119.16422793**、funding 10.59291420→**3.79529312**，而**成交笔数/胜负/胜率一个没动**（fills 123、closed 122、open 1、wins 32、winRate 26%）——换算只缩放数量、不改变策略的进出场决策，这本身就是「敞口是纯缩放因子」的一条证据；`BacktestRunnerTest` 手算量 29.702→**9.900**（0.10×10000/101 floor 到 stepSize 0.001）。手算 0.30 的三个测试（`PositionSizerTest` 字段、`RiskGateTest` harness、及 `aSmallerTargetExposure…`）改成**显式** `new PositionSizer.Policy("0.30")` 而不是 `DEFAULT`——它们验的是换算/编排语义不是出厂配置，钉死一个不随 §8 上限变动的数才对；`DEFAULT` 的值改由 `PositionSizerTest.defaultsTargetExposureLeavesRoomForAFlip` 单独钉住并写下耦合理由。**clean 重建炸出一个静态初始化环**（增量构建因命中旧 `.class` 而从未暴露）：`Risk.DEFAULTS` 的静态初始化会跑紧凑构造器去读各层的 `DEFAULTS`，而各层紧凑构造器又调用 `Risk.require(...)`——只要 JVM 先初始化了某个嵌套层（例如拒绝类用例先 `new Account(...)`），该层的 `<clinit>` 就会经 `require` 触发 `Risk.<clinit>`，此时该层的 `DEFAULTS` 还在赋值右值的 `new` 里、仍是 null，于是 `Risk.DEFAULTS.account()` 被永久固化成 null，读它即 NPE。**是否中毒只取决于哪个类先被初始化**，所以同一批用例时而全绿时而两个 NPE（`-Dtest=RiskPropertiesTest` 单跑 clean 可稳定复现）。修法是把 `require` 从 `Risk` 移到外层 `AlphaProperties`：`AlphaProperties` 没有静态字段，其 `<clinit>` 平凡，嵌套层回调它不会再入环，两种初始化次序都得正解；移动保持了消息与语义逐字不变，`RiskPropertiesTest` 的拒绝用例照常杀。**变异测试 8/8 全杀**（耦合不等式 `<0`→`<=0`、耦合守卫删除、组合层 symbol>total 守卫删除、`enabled` 归一删除、`require` 下界 `<=0`→`<0`、`effectiveTargetExposure` 的 null 分支删除、breaker `consecutiveLosses<=0`→`<0`、frequency `maxOrders<=0`→`<0`），每个都有直接对应的具名用例。 |
| 2026-09-10 04:20 | T303（账户级规则 `AccountRule`：总杠杆上限 3x + 保证金率下限 150%，信号阶段） | 通过：新增 11 个单测（`AccountRuleTest`），`mvn -B -o clean verify` 全绿（alpha-risk 含新测、alpha-app 73+1 skip 不变）。**账户级跑在信号阶段（pre-size）、读当前账本**：此时还没有数量，所以它是「账本当前是否健康」的门，而不是「本单是否过大」——后者是订单级/组合级（post-size）的职责；与 T301 把 `ACCOUNT` 划进 `SignalRule` 一致。两个阈值都用**交叉相乘而非除法**（精确、无舍入、边界不漂，且 rejection 能原样引用触发它的那条不等式）。**取舍 12 在此落地**：`minMarginRatio` 是本系统自己的定义 `equity/usedMargin`（`usedMargin=totalNotional/maxLeverage`，越高越健康），与 Binance 同名指标方向相反；规则自算，交易所那个字段只进对账（T318），直接对接会放过快爆仓的单。**先查杠杆上限（CRITICAL）再查保证金下限（WARNING）**：`usedMargin` 除以 `maxLeverage`，下限等价于 `maxLeverage/leverage`，出厂数下 `3/1.5=2x` 比 `3x` 上限更紧——开仓在杠杆过 2x 时就停（margin 早预警），3x 硬上限是给「靠价格波动而非开仓爬到那里」的账本兜底；先查上限意味着超过 3x 的账本报硬停而不是软预警，用例 `leverageExactlyAtTheCeilingIsNotALeverageBreach`（恰好 30000 出来的是 margin WARNING）证明上限检查在该点确实放行。**只拦开仓**：`FLAT` 永远放行（不健康账本必须能减仓，拦退出等于把它锁进触发规则的那笔敞口），`LONG`/`SHORT` 都拦（都是承担方向性敞口）；pre-size 没有数量，无法把「净减」的翻仓与「净增」的翻仓区分开，对风控门保守的选择是拦下、让策略先 FLAT。**非正权益走显式分支放行且该分支承重**：`equity<=0` 是 sizer 的 `RULE_NO_EQUITY`，这里也认领会给同一事实两个 owner、拦截记录里两个 ruleId；它承重是因为 0 权益否则会让 `totalNotional > maxLeverage×0 = 0` 触发一条假 CRITICAL（用例 `noEquityIsLeftToTheSizerRatherThanDoubleClaimed` 钉 0 与 -100）。**变异测试查出一处冗余守卫并删掉**：flat-book 守卫（`totalNotional<=0 → empty`）的变异体存活——查证 `Portfolio.totalNotional()` 恒非空非负（`zero()` 起、累加 `.abs()`），唯一可达输入是 0，而 0 在阈值算术下两边都不触发（0 不 `>` ceiling；`equity×maxLev < ratio×0=0` 对正权益恒假），所以这是冗余守卫而非弱测试，按纪律删除而不是用不可能的输入硬凑一个能杀它的测试；javadoc 随之改写，明确区分「权益守卫是显式承重分支」与「flat book 由算术天然放行」。**变异测试 11/11 全杀**（杠杆边界 `>`→`>=`、杠杆检查删除、杠杆 severity CRITICAL→WARNING、保证金边界 `<`→`<=`、保证金检查删除、保证金 severity WARNING→CRITICAL、FLAT 放行删除、FLAT→LONG、非正权益放行删除、两个构造期守卫删除）；其中「杠杆检查删除」与「保证金检查删除」两个变异体共同钉死检查顺序——删掉任一条，30000.01 处就只剩另一条触发，severity 与 detail 都会与用例断言不符。 |
| 2026-09-10 04:32 | T304（订单级规则 `OrderLimitsRule`：单笔名义上限 20% + 价格偏离 2% 乌龙指，订单阶段） | 通过：新增 15 个单测（`OrderLimitsRuleTest`），`mvn -B -o clean verify` 全绿（alpha-risk 73、alpha-app 73+1 skip 不变）；`BacktestSmokeTest` 黄金值不动——规则尚未进管道，装配是 T308 的事。**给 `OrderFacts` 加了一个可空 `limitPrice`**（null ⟺ MARKET）：T301 刻意没放订单价，其 javadoc 论证的是「名义 cap 不能坚持要真实价、否则在只发 MARKET 的网关上永不触发」——那条论证针对的是名义，不是偏离；偏离检查本就是关于限价单的，MARKET 无价可比、正确行为是跳过。新增 4 参 `of(facts,side,qty,limitPrice)`，保留 3 参 `of` 委托传 null，于是 `RiskGate` 与 `RiskPipelineTest` 的 6 处既有调用一字未改。**名义仍按 mark 估算、不随 `limitPrice` 切换**：cap 必须在网关真正发的单上触发，且在偏离带内的限价单名义至多差一个带宽，对 20% 的 cap 无关紧要；`limitPrice` 只喂偏离检查。**两检查共用一个 id `RK-03-order`**（与 `AccountRule` 的「一个 level 一个 id、detail 说明哪条触发」一致，`attributed` 守卫要求一规则一 id 一 level）。**偏离先查（CRITICAL）再查名义（WARNING）**：乌龙价是「输入不可信」（可能 bug 或手抖，值得 page），名义超限是「政策尺寸」（gate 正常履职），先查偏离 => 同时违反两条的单报 CRITICAL 而不被 WARNING 掩盖，用例 `deviationIsCheckedFirstSoACriticalPriceIsNotMaskedByAWarningSize`（limit 110 偏 10 + 名义 3000 超限）钉死。**名义 cap 豁免减仓单——这是本任务查出的真实正确性问题，不是扩范围**：sizer 目标 10%，但一个持仓可仅靠价格上涨涨过 20% 权益，平掉它是「一笔名义等于整个持仓」的单、会超 cap；拦它就把持仓困死，正是 `AccountRule` 用 FLAT 放行要避免的陷阱。故 cap 只约束**新增**名义：`projectedSignedQty` 绝对值**严格小于**当前 => 减仓 => 放行；翻仓到同量或更大量不算减仓、仍受 cap（用例 `aFlipToALargerMagnitude…`、`aFlipToTheSameMagnitude…`）。偏离检查对减仓单**不**豁免——以乌龙价平仓和以乌龙价开仓一样糟。**两条检查在出厂配置下都是 backstop，这是设计而非偶然**（正面回应 `OrderFacts` javadoc 那句「looks configured and never runs」的警告）：名义 cap 与 sizer 耦合（取舍 17 `cap ≥ 2×targetExposure`），而 sizer 至多要 `2×targetExposure×equity`（满强度翻仓），故正常尺寸的单恰在 cap 或以下——cap 是把那条耦合从「期望」变成「不变量」的唯一执行点，并对任何未来来源的超限单触发；偏离守卫在只发 MARKET 时休眠，引入 LIMIT 即生效。两者都正确、都被「违反它的单」的单测覆盖、都是该条件唯一执行点，所以不是死规则。**equity/mark 在此阶段保证可用**：sizer 已在订单阶段之前拒掉 `RULE_NO_EQUITY`/`RULE_NO_PRICE`，本规则不复查（一条件一 owner，同 `AccountRule` 对 no-equity 的处理）。边界用交叉相乘精确判定：恰好 20%（满翻仓）放行、恰好 2% 两侧放行。**变异测试 15/15 全杀**（名义边界 `>`→`>=`、名义检查删除、名义 severity→CRITICAL、名义改用 `qty` 而非 `orderNotional`、偏离边界 `>`→`>=`、偏离检查删除、偏离 severity→WARNING、偏离去 `.abs()`（下方偏离变负不触发）、偏离带宽当绝对值不乘 mark、减仓豁免删除、减仓 `<=` 代替 `<`（翻仓到同量被误判为减仓）、减仓比较反向、减仓漏掉 `signedQty` 的 `.abs()`、两个构造期守卫删除）；其中「偏离检查删除」与「偏离 severity→WARNING」共同钉死「偏离先于名义」的顺序，而「减仓漏掉 `signedQty().abs()`」只被**空头平仓**那条用例杀掉（多头平仓 `0<30` 与去 abs 后 `0<30` 同真，测不出来）——这正是补 `aShortPositionClosingIsExemptTheSameWay` 的理由，与 T301「空头是符号 bug 唯一藏身处」同一教训。 |
| 2026-09-10 04:45 | T305（组合级规则 `PortfolioRule`：总持仓名义 ≤60% 权益 + 单 symbol ≤30%，订单阶段） | 通过：新增 14 个单测（`PortfolioRuleTest`），`mvn -B -o clean verify` 全绿（alpha-risk 87、alpha-app 73+1 skip 不变）；`BacktestSmokeTest` 黄金值不动——规则尚未进管道，装配是 T308 的事。**把 T304 内联的减仓判定上提为 `OrderFacts.reduces()` 单一共享谓词**（`projectedSignedQty.abs() < signedQty.abs()`），`OrderLimitsRule` 与 `PortfolioRule` 都调它——两条订单阶段规则若各算各的「什么算减仓」，迟早会在翻仓到同量、翻仓到更大量这类边界上分叉，把判定放进 `OrderFacts` 让它们结构上无法不一致。**组合级读「成交后的账本投影」而非当前账本**：查当前总名义会放过「一步一步把已超限的账本继续做大」的每一单，查本单名义会漏掉「相对权益很小但把单一标的翻倍」的单——投影是这一层全部价值（同 T301 `OrderFacts` 的论证）。**减仓单永不被这两条拦**（验收硬要求）：总 cap 与单 symbol cap 都只约束新增敞口，平掉一个已超限的持仓是「一笔名义等于整个持仓」的单、必然超 cap，拦它就把账本困死在触发规则的那笔敞口里（同 `AccountRule` 用 FLAT 放行、`OrderLimitsRule` 名义 cap 豁免减仓的同一条去风险豁免原则）；用例 `aReductionIsExemptEvenWhenTheBookStaysOverBothCaps`（signedQty 50 + other 2000，SELL 5，成交后总名义仍远超 60% 与 30%）钉死「即便账本成交后依旧双双超限、减仓单照样放行」。**总 cap 先查再查单 symbol cap**：同时违反两条的单报更宽的总持仓限制，用例 `anOrderBreachingBothCapsReportsThePortfolioCapFirst` 断言 detail 含 "portfolio total cap" 且不含 "single-symbol"，两个检查各自承重。**两条都是 WARNING**（政策尺寸，gate 正常履职），危险（杠杆过 3x）是 `AccountRule` 的 CRITICAL——severity 哲学一致：WARNING = 订单/信号被政策拒绝、账户无恙，CRITICAL = 账户有险/输入不可信/无法计算。**构造期守卫 `maxSymbolFraction ≤ maxTotalFraction`**：单 symbol 上限比总上限还松是无意义配置（单标的永远先被总 cap 拦下、symbol cap 成死规则），相等是合法的（单一标的账本），用例 `aSymbolFractionLargerThanTotalIsRefusedButEqualityIsAccepted` 钉两侧。**两条在出厂配置下同样是 backstop**（正面回应 `OrderFacts` javadoc「looks configured and never runs」警告）：sizer 目标 10%、总 cap 60%，正常尺寸的单到不了 cap；它们是把「敞口受控」从期望变成不变量的唯一执行点，并对任何未来来源（手动下单、策略 bug、价格漂移把持仓推过线）的超限单触发——都被「违反它的单」的单测覆盖，不是死规则。**变异测试 18/18 全杀**：Phase A 13 个（总 cap 边界 `>`→`>=`、总 cap 检查删除〔钉死总先于 symbol 的顺序〕、总 cap severity→CRITICAL、总 cap 误用 `projectedSymbolNotional`、symbol cap 边界 `>`→`>=`、symbol cap 检查删除、symbol cap severity→CRITICAL、symbol cap 误用 `projectedTotalNotional`、减仓豁免删除、三个构造期守卫删除、symbol>total 守卫边界 `>`→`>=`）；Phase B 5 个（`reduces()` 恒假、`<`→`<=`〔翻仓到同量被误判为减仓〕、比较反向、漏掉 `projectedSignedQty` 的 `.abs()`、漏掉 `signedQty` 的 `.abs()`）——Phase B 跑 `OrderLimitsRuleTest,PortfolioRuleTest` 两套，证明上提后的 `reduces()` 仍被两条规则的用例共同钉住，与 T304 内联版杀伤力等价（去 abs 的两个变异体仍只被空头平仓用例杀掉，同 T301/T304 教训）。 |
| 2026-09-10 04:58 | T306（熔断级 `CircuitBreaker`：单日亏损 -5% 当日禁止开仓 + 连亏 3 笔停 2h，信号阶段） | 通过：新增 20 个单测（`CircuitBreakerTest`），`mvn -B -o clean verify` 全绿（alpha-risk 107、alpha-app 73+1 skip 不变）；`BacktestSmokeTest` 黄金值不动——规则尚未进管道，装配是 T308 的事。**这是五级里唯一有状态、且唯一读 `Portfolio` 的规则，两个角色一个实例**：作为 `SignalRule` 它只从 `SignalFacts` 决策、不碰账本（守住 T301 的单快照原则——facts 就是拦截记录要描述的账户状态）；作为 `EventHandler` 挂在总线上观察 `FillEvent` 以在两个信号之间维持状态。`RiskRule` 的 javadoc 早就为「保持状态的规则（频率计数器、熔断器）」开了口子，但「从不碰账本」那句是针对 `check()` 的纯决策契约——熔断器只在**观察者角色**里读 `Portfolio`，且只读不写。**为什么必须读账本**：`FillEvent` 不带已实现盈亏（它是账本的属性、不是成交的属性，同一笔成交在不同的持仓下实现不同的盈亏），所以「这一笔是赚是亏」只能从 `Portfolio.realizedPnl()` 自上次成交以来的**增量**还原；这成立的前提是账本在 `FillEvent` 发布**之前**已更新（取舍 16）、且引擎单线程，于是熔断器按序看到每一笔成交、中间没有别的成交插进来。构造时 `lastRealizedPnl = portfolio.realizedPnl()`，让注册之前发生的成交不被算到它头上（生产中熔断器在装配期注册、账本尚新，此值是 0，纯属防御）。**两个触发器彼此独立**：①单日亏损——一旦权益较 UTC 日起点跌掉 `dailyLossFraction`（5%），当日**剩余时间**禁止开仓（全天禁止开仓），**闩锁是刻意的**：一个随权益反弹就解锁的熔断器会让剧烈波动的一天把它反复开关，而「今天亏了 5%」是关于这一天的陈述、不是关于此刻的；②连亏——`consecutiveLossLimit`（3）笔连续亏损交易暂停开仓 `pause`（2h），连亏这么长是策略在跟市场作对，暂停是**计时**的（到点自动恢复）而非等到回本。**只拦开仓**：`FLAT` 永远放行（同 `AccountRule`/`OrderLimitsRule`/`PortfolioRule` 的去风险豁免——触发熔断的账户必须还能减仓，拦退出等于把它困在触发规则的那笔敞口里）；`LONG`/`SHORT` 都是承担方向性敞口、触发期间拒绝；pre-size 没有数量，无法把净减的翻仓与净增的翻仓分开，对熔断器保守的选择是拒绝、让策略先 FLAT。**日亏损闩锁在每次观察（每笔成交 + 每个信号）都重评**，所以盘中低谷即便没有信号正好落在谷底也能被抓到；权益是 mark-to-market 的，一个只亏浮盈、一笔成交都没有的持仓也能触发熔断。**两个观察入口共用 `observe(now, equity)`**：先翻 UTC 日界（新的一天把 `dayStartEquity` 重设为当前权益、清空闩锁——熔断器按每个 UTC 日各自计量），再对当前权益样本重评日亏损闩锁。`check()` 用 `facts.equity()`（守单快照），`onFill()` 用 `portfolio.equity()`（成交后），两者读的是同一个账本、一致。**severity 沿用闸门那一条规则**：日亏损触发是 CRITICAL（实打实回撤 5% 是账户有险），连亏暂停是 WARNING（启发式计时器，三笔小亏并不危及账户）；CRITICAL 先查，于是一个既跌 5% 又连亏的日子报回撤而不是报那个更软的计时器（用例 `theDailyLossIsCheckedFirstSoACriticalDrawdownIsNotMaskedByAWarningPause`，-170×3=-510 同时触发两条，断言 CRITICAL 且 detail 含 "daily loss" 不含 "consecutive"）。**暂停跨日界存活、日亏损闩锁不跨**：暂停是计时的（23:00 触发→次日 01:00 恢复），日亏损是按日的（翻日即清）；用例 `thePauseSurvivesADayRolloverButTheDailyLatchDoesNot` 在午夜前 1h 触发连亏暂停、推进到次日 00:30（已翻日、暂停仍在），断言出来的是 WARNING "consecutive" 而不是被翻日清掉的日亏损。**触发即消费连亏计数**（`consecutiveLosses = 0` after 设定 `pausedUntil`）：暂停是这一串连亏的后果，计数越过上限只会让它在到期时立刻再触发；消费掉之后，暂停期满需要**全新的一串**连亏才能再停（用例 `aFreshStreakIsNeededToPauseAgainAfterThePauseExpires`：触发暂停→推进到期满→再 2 笔亏损（不足 3）→放行；若去掉这个重置，旧计数会让期满后**任意一笔**亏损立即再停、永远恢复不了）——这一条是变异测试逼出来的真实正确性点，不是扩范围。**开仓/加仓实现 0、不算亏损交易**：`realized == 0`（开仓、加仓、保本平）既不是赢也不是亏，连亏计数**原样不动**——两笔亏损之间夹一个开仓不会把计数清零（用例 `openingsAndAddsRealizeNothingAndDoNotMoveTheStreak`）；赢一笔（`realized > 0`）才清零（用例 `aWinningTradeResetsTheStreakSoItNeverReachesTheLimit`：亏2→赢1→亏2，从未连续到 3，放行）。**时间全部来自 facts/事件、不持有 Clock**：`check()` 用 `facts.nowMillis()`（闸门已捕获的同一瞬），`onFill()` 用 `fill.timestamp()`（成交的业务时间，比观察时刻更准）；测试用 `VirtualClock` 定点推进来驱动这些时间戳（验收要求），回测里所有时间来自数据、绝不来自 `System.currentTimeMillis()`，所以暂停到期时刻、UTC 日界、`Instant.ofEpochMilli` 格式化进 detail 的字符串全部确定、不进任何被逐位比对的产物（NFR-04）。**变异测试 20/20 全杀**：日亏损边界 `>=`→`>`、日亏损触发块删除、日亏损 severity CRITICAL→WARNING、日亏损闩锁改成实时（回本就解锁，杀于 `theDailyLatchSurvivesAnEquityRecovery`）、连亏边界 `>=`→`>`、`consecutiveLosses++` 删除、赢单清零删除、亏损判定 `<0`→`<=0`（开仓被当成亏损，杀于 `openingsAndAdds…`）、暂停到期 `<`→`<=`、暂停检查删除、FLAT 豁免删除、FLAT→LONG、`check` 里日亏损检查删除（钉死日亏损先于暂停的顺序）、`check` 里不调 `observe`、翻日不清闩锁、触发即消费计数删除、四个构造期守卫删除。 |
| 2026-09-10 05:12 | T307（频率级 `FrequencyRule`：滑动窗口 ≤10 单/分钟，信号阶段最后一条） | 通过：新增 10 个单测（`FrequencyRuleTest`），`mvn -B -o clean verify` 全绿（alpha-risk 107→**117**、alpha-backtest 125、alpha-app 73+1 skip 不变）；`BacktestSmokeTest` 黄金值不动——规则尚未进管道，装配是 T308 的事。变异测试 **20/20 全杀**（`/tmp/mutate_t307.py`）。**槽位是「信号」不是「订单」**：pre-size 还没有数量，数不了订单，只能数「决定要交易」这件事，这是一个偏保守的代理（被换算成 no-trade 的、以及之后被订单级/组合级拦掉的，都白占了一个槽）——但保守是对的那一边，而且一分钟发十个空信号本身就是失控，报警正是这一级存在的目的。**曾经想让它兼做 `EventHandler` 去观察 `OrderRequestEvent`，好精确地数订单，读完 `EventEngine.dispatch` 放弃了**：handler 发布的事件进的是 cascade 队列（`handler.onEvent(event, cascade::add)`），同一轮里若一个策略发出多个信号，它们会被依次 dispatch，而订单事件排在剩余信号事件**之后**——恰恰是突发那一轮计数会滞后，正好漏掉「突发拦截」这条验收。所以占位改成在 `check()` 里同步完成。**拒绝不占位**（只在通过时 `addLast`），否则一分钟限速会退化成永久停机：被拒的信号若也进 deque，窗口滑出的速度赶不上堆积的速度。**这是五级里唯一不豁免减仓/FLAT 的一级**：别的层豁免是因为拦住平仓会把账户困在触发规则的那笔敞口里；这一层限的是交易所速率预算，平仓单一样花预算，而且封锁以窗口为上界、下一根 bar 就会重发，困不住任何东西——反过来豁免 FLAT 等于给失控循环留一条「只要喊平仓就能全速发送」的后门。**一个窗口覆盖全部 symbol**（不是每 symbol 一个）：被配给的是账户在交易所的速率预算，它本来就是账户级的，每 symbol 一个窗口正好放过十 symbol 扇出的那种突发。**实现上的两个坑**：①窗口取 `(now-window, now]`，恰好一个窗口老的槽位算已滑出（单测在 59999ms / 60000ms 两侧钉死）；②清槽用 `removeIf` 全扫而不是从队头 drain——正常情况下 deque 有序，但实盘 NTP 把墙钟往回拨会在队头留下一个未来时间戳，从头 drain 就什么都清不掉，规则会永远卡在上限；O(maxOrders)=10 的代价换一个不会永久停机。**构造守卫是 `window.toMillis() >= 1` 而不只是 positive**：亚毫秒窗口截断成 0ms 会让每个槽位一到就被清掉，规则被配置了、永不触发、也永不报警——`AlphaProperties.Frequency` 只挡了 non-positive，这条更细的守卫放在规则自己的构造里。20 个变异体逐条：上限边界 `>=`→`>`、上限检查删除、上限写成常量 1、通过不占位、拒绝也占位（`addLast` 提到判定之前）、清槽边界 `<=`→`<`、永不清槽、总是清槽、窗口塌成 0、窗口翻倍、`freesInMillis` 改用最新槽位（钉死报警说的是「最早那个槽位多久后空出来」）、severity→CRITICAL、拒绝的 level→ORDER、`level()`→ORDER、`ruleId()`→别的、`RULE_ID` 改名、四个构造期守卫（含两处边界）。 |
| 2026-09-10 05:26 | T308（**SC-04 拦截矩阵** + 五级管道真正接进出厂装配） | 通过：新增 `RiskInterceptionMatrixTest`（10 用例 / 7 行矩阵）、`RiskPipelinesTest`（5）、`BacktestRunnerTest.aRuleThatWatchesTheBusIsRegisteredOnIt`、`BacktestWiringTest.theShippedPortfolioCapStopsAnOversizedOrderInARealRun`，`mvn -B -o clean verify` 全绿：alpha-risk 117→**127**、alpha-backtest 125→**126**、alpha-app 73→**79**（+1 skip 不变），全仓 437→**454**。变异测试 **26 个变异体：25 杀 / 1 存活 / 0 无效**（`/tmp/mutate_t308.py`）；再对 9 个 guard 变异体做**只跑矩阵**的第二遍（`/tmp/mutate_t308_matrix.py`）得 **7 杀 / 2 存活**，两个存活体恰是矩阵 javadoc 断言「真实闸门到不了」的那两条 guard，存活本身就是那条可达性论断的实证。**结构问题与解法**：`CircuitBreaker` 要读运行期账本（从 `Portfolio.realizedPnl()` 的增量还原每笔盈亏），而账本在 `BacktestRunner.run()` 里才由 `initialCash` 创建 —— 于是 `Config` 收的不是 `RiskPipeline` 而是新增的 `RiskPipelineFactory`（`create(Portfolio)`，`fixed()` 给不需要账本的规则集）。**没有把账本创建上提到调用方**：那样两次 `run()` 会共用一本脏账（`Strategy` 带状态、SC-02 三次重放要求每次重建），而工厂只改了一个外部调用点（`BacktestWiring`）。**查出一个真实的正确性漏洞**：闸门只**咨询**规则、从不把事件转发给它们，所以挂在总线上的规则必须单独注册 —— 原装配里没有这一步，出厂熔断器的**连亏触发器会静默失效**（日亏损那条照常工作，因为它在 `check()` 里自评，于是症状是「规则响得变少」而不是报错）。修法是 `registerRuleObservers(engine, pipeline)`，**两个阶段都扫**，这样将来某条订单级规则需要总线时不会被静默漏掉；新用例把两个阶段各放一个既是规则又是 `EventHandler` 的 watcher，断言两者都看到 `FillEvent` 且各被咨询一次（注册不能顶替管道自己对它的使用）。**黄金值移动并按 PROVENANCE 实测归因**：fills 123→**117**、closed 122→116、wins 32 不变、losses 90→84、totalReturn -4.56%→**-4.07%**、maxDrawdown 5.45%→**4.97%**、fees 119.16→113.74、funding 3.795→4.225 —— 熔断暂停抹掉 6 个来回、且这 6 笔全是亏损（wins 不动）。归因是**测出来的不是推出来的**：只把 `alpha.risk.breaker.enabled=false` 一个开关关掉，十个数字**逐一精确回到 P3 之前的旧黄金值**，这同时证明另外四级在这 2400 根 bar 上从未触发（10% 目标敞口让账户离每一道 cap 都还差一个数量级）。**矩阵设计**：7 行覆盖 5 级，account 与 breaker 各占两行以钉死「一个 level 一个 id、detail 说明哪个检查响了」—— 每行断言**无订单 + 恰好一条告警 + ruleId + severity + 规则自己措辞的字面片段**，那段措辞才是真正的短路点（`RK-02-account` 同时拥有杠杆上限 CRITICAL 与保证金下限 WARNING，id+severity 之后仍剩两个检查）；另有①聚合用例断言五级齐全、拦截率 7/7=100% 并把矩阵打进构建日志（SC-04 是验收项，得能被人从日志里读出来），②**两级同时反对时报配置顺序里靠前的那一条**（账本既超 3x 硬上限、速率窗口又已满，告警必须写 account/CRITICAL —— 换个顺序跑规则照样拦得住，却会把单子归档成「发得太快」，让读告警的人去查错东西），③**对照组**：一个干净信号穿过五级变成一张 MARKET 单（没有它，「拦截率 100%」也正是一个什么都不放的闸门能拿到的成绩，而把某条规则改成恒拒绝的变异体会让这个文件更绿而不是更红）。**两条关于可达性的诚实结论，写进 javadoc 而不是藏起来**：①单笔名义 cap（20%）在出厂配置下**穿过闸门到不了** —— 取舍 17 强制 `targetExposure ≤ 0.10`，满强度翻仓恰是一笔 `2×0.10×权益` 的单、正落在 cap 上而 cap 是严格 `>`，任何更大的单都是减仓、被 cap 刻意豁免；所以 ORDER 行把敞口抬到 0.15（一个 `AlphaProperties.Risk` **拒绝绑定**的配置），理由直说：cap 是把那条耦合从「期望」变成「不变量」的唯一执行点，而**没有这一行，删掉 cap 在整个矩阵里不会让任何用例失败**。②单 symbol cap（30%）同理不可达（sizer 目标至多 10%，翻仓至多两倍且先被 20% 订单 cap 拦下，被行情推过 30% 的持仓永远被 sizer 往回减 => `reduces()` 豁免），**矩阵不为它编造一行**，它由 `PortfolioRuleTest` 用手搭的 `OrderFacts` 覆盖 —— 第二遍变异里 `guard-portfolio-symbol-cap` 与 `guard-order-price-deviation`（闸门只发 MARKET，`limitPrice` 恒 null）正是仅存的两个矩阵存活体，逐个确认失败的行也是对的（`[5]` FREQUENCY、`[7]` PORTFOLIO，外加聚合的拦截率断言）。**唯一的存活体是等价变异体**：`runner-observers-registered-last`（把观察者注册挪到策略引擎之后）杀不掉，因为读 `EventEngine.dispatch` 可知**整个 handler 循环跑完才 drain cascade**，所以某个 handler 在列表里的位置**不可能**改变「由 FillEvent 级联出来的信号被谁拦」—— 这条发现反过来纠正了 `BacktestRunner` javadoc 里一句把注册位置说成承重的话，已改成「位置是为了可读性，不是正确性；保证来自 cascade 语义」。**另一个坑**：`new AlphaProperties.Risk.Breaker(false, null, 0, null)` 构造即抛 —— 关掉一级**不等于**关掉校验，`consecutiveLosses >= 1` 与 `pause > 0` 在 `enabled=false` 时照样执行（正确：否则一个关掉的熔断器会在被人打开的那一刻才炸），所以「关掉某级」的用例必须带合法数字、只动那一个布尔。26 个变异体逐条：管道删五级各一条（5）、五级 `enabled` 判断改成恒真（5，钉死「关掉一级是**缺席**而不是在场且宽松」）、信号阶段 account 与 breaker 换位、订单阶段 order 与 portfolio 换位（各钉死顺序即契约）、熔断器改用一本没人写的账本（`new Portfolio(startingEquity)`，杀于 `theBreakerDecidesFromTheBookItWasHanded` **和**冒烟黄金值）、runner 删注册调用 / 只扫信号阶段 / 只扫订单阶段 / 挪到最后（4）、九个 guard 逐个 `if (false)`（account 上限、account 保证金下限、breaker 日亏损、breaker 连亏、frequency 上限、order 名义 cap、order 偏离、portfolio 总 cap、portfolio symbol cap）。 |
| 2026-09-10 05:36 | T309（持久化契约：`OrderStore` / `RecordStore` + 内存实现 + 依赖红线扫描） | 通过：新增 execution 三类（`OrderRecord`/`OrderStore`/`InMemoryOrderStore`）、risk 五类（`InterceptionRecord`/`EquitySnapshot`/`PositionSnapshot`/`RecordStore`/`InMemoryRecordStore`），新增 `OrderRecordTest`(7)、`InMemoryOrderStoreTest`(7)、`InMemoryRecordStoreTest`(7)、两个 `DependencyRedLineTest`；`mvn -B -o clean verify` 全绿：alpha-execution 0→**15**、alpha-risk 127→**135**，全仓 454→**477**。变异测试 **26/26 全杀 / 0 无效**（`/tmp/mutate_t309.py`；`/tmp/mutate_one.py <name>` 可单跑一个变异体复查）。**红线不 grep 源码，扫编译产物的常量池**：`Files.readString(classFile, ISO_8859_1)` 把字节一对一映射成字符，于是 `contains` 就是字节搜索，找 `java/sql/`、`javax/sql/`、`org/springframework/`。不用 grep 的理由：一条 import 可以被删掉而引用仍在（全限定名），一个由注解或反射进来的依赖照样落在常量池里；反过来源码注释和字符串里写了 sql 也不该算。**并且证明它非空跑** —— 注入 throwaway `Scratch` 类分别引用 `java.sql.*` 与 `javax.sql.DataSource`，读到 `Scratch.class -> java/sql/`（execution，日志 "5 classes scanned"）与 `Scratch.class -> javax/sql/`（risk，"26 classes scanned"），确认两个文件随后被删干净。断言消息里带 scanned 计数是必要的：一个扫到 0 个 class 的红线测试永远绿。**`OrderRecord` 是状态不是历史**：spec 说「每次迁移落库」，落的是这一行被重写，不是每次迁移长一行 —— 迁移史已经在事件日志里（一次迁移一条 `OrderUpdateEvent`），再加第七张 `order_transitions` 表就是给同一个事实两个家，而两个家迟早不一致（取舍 15 已钉死六张）。**这个 record 也不判合法性**：`after()` 照样能产出 FILLED→SUBMITTED，状态机属于 T312 的 OMS —— 只有在那里它才可测（要总线、要「非法迁移一律拒绝」的语义），放在数据行里只会得到一个没人调用得到的 guard。**四个可空列的 null 是信息不是缺省**，所以 typed 而不是填零：`avgFillPrice` 为 0 会读成「以 0 价成交」，一张 `exchangeOrderId` 为空串的挂单会让对账无从问起。**`InterceptionRecord` 存 `SignalFacts` + `RiskRejection`，不是存 `RiskAlertEvent`**：告警只带 ruleId/severity/detail，一点账户信息都没有 —— 存告警的库能查、能说出命中哪条规则，却答不了「这次拒绝对不对」，而且看起来是完整的，这正是它危险的地方。**由此得出一条装配硬约束（T312-T314 必须遵守）：这条记录只能由 `RiskGate` 自己写库，不能挂在总线上**，因为 `SignalFacts` 只存在于一次 `onEvent` 调用内部、从不发布，任何注册顺序都还原不出它（`RecordStore` 因此要注入进闸门）。**`EquitySnapshot` 刻意只存一个数**：决策瞬间的明细是 `InterceptionRecord` 的职责；一张也带 cash/notional/P&L 的快照表就是第二本账，按定时器写、大部分时候与第一本一致，而不一致的那天没有任何办法说出是哪本先动的。**`PositionSnapshot` 必须带 `markPrice`**：它是这一行里唯一事后无法重建的输入 —— 用后来的标记价重估过去的持仓，正是持仓史悄悄开始说谎的方式（当天撑破上限的那笔名义，事后重估就不再破了，于是 FR-EX-04/06 的纠正与一个动了账本的 bug 变得无法区分）。平掉的 symbol 要不要落行是采样器的决定，诚实的默认是 `Portfolio.openPositions()`。**`RecordStore` 四张表放一个接口、住在 risk**：四分之三是总线上已有的事实，第四张需要 `SignalFacts`，所以只能在 risk；一个 owner = 一份 DDL、一个事务边界、一个「这套系统到底记了什么」的答案，拆成两半的结果是两个 JDBC 类写同一个文件。**四张都是 append-only 历史，与 `orders` 形状不同，方法签名把这件事显出来**（`orders` 是状态 → find/findOpen；`fills` 是历史 → by order；一个 fill 就以 `FillEvent` 本身入库，不再定义第三个类型）。区间两端都含，与 `KlineRepository.load` 一致；代价（相邻窗口共享边界那一瞬）写进 javadoc，也是这里不分页的原因。**一个真实的领域误解被测试抓出来**：`expected: 9000 but was: 10000.00000000`。`Portfolio` 是**保证金账本，不是现金账本** —— 开仓既不动 `cash` 也不动已实现盈亏，现金只因已实现盈亏和手续费变化。断言改成 cash 10000 / equity 9940 / notional 940（10 BTC @100 开仓、标记跌到 94），并把这条事实写进用例注释而不是只留在提交信息里。**两个存活体，两种处理**：`after-avg-price-not-coalesced` 是真漏洞（没有任何用例在设过 avgFillPrice 之后再迁移一次）→ 补 `cancelingTheRemainderKeepsTheAveragePriceOfThePartThatFilled`（SUBMITTED→PARTIALLY_FILLED 0.0500@68000.10→CANCELED「client canceled the remainder」，断言 filledQty 与 avgFillPrice 都还活着：0.05 确实成交了，那是已经动过的钱，撤掉剩下的撤不回它），复跑确认已杀。`after-message-not-coalesced` 诊断为**在合法生命周期里不可达**（statusMessage 只随 REJECTED/CANCELED 到达，二者皆终态，终态之后不再有迁移）—— 但没有把「不可杀」记一笔就算完，而是补 `aNullArgumentMeansTheExchangeSaidNothingAboutThatColumn`，在一次交易所什么列都没提的迁移上把 `after()` javadoc 承诺的「null = 不变」四条统一钉死（四列中三列本就被生命周期用例钉住，第四列此前只是碰巧）。**为 T310 查出两条约束**：①内存契约承诺插入序（`findOpen` 的 `containsExactly`、`fills` 的乱序存正序读都靠它），而 SQL 没有插入序 —— 所以 JDBC 历史表需要显式的单调排序列；`(business_ts, event_id)` 已经是房规（`EventIds` 的 javadoc：跨进程顺序由 (timestamp, eventId) 重建），代理自增列不算方言中立（SQLite `AUTOINCREMENT` 与 MySQL `AUTO_INCREMENT` 语法分裂，且重启后要拿 `MAX(seq)` 播种才不撞已落库的行）。②`JdbcKlineRepository.decimal()` 遇 NULL 直接抛，而 `OrderRecord` 有四列可空，T310 需要一个可空变体而不是放宽那一个。**内存 round-trip 测试是照着接口写的，因为一个知道 SQLite 的 round-trip 测试测的是 SQLite**；其中 `isEqualTo` 那几条（AssertJ 对 BigDecimal 是 scale 敏感的）今天在内存实现上平凡通过，指向 NUMERIC/DECIMAL 列的那天会响亮地失败 —— 现在写下它就是为了那天（取舍 5/15 把价格存成文本的全部理由）。**一个环境坑**：`mvn -pl alpha-execution` 不带 `-am` 会报 `Could not find artifact ...alpha-engine`，而这个 BUILD FAILURE 在变异脚本里会被误判成 KILLED（分类只看有没有 BUILD SUCCESS / COMPILATION ERROR）。修法是改完主源签名先跑一次 `mvn -B -o clean install -DskipTests`，让单模块离线构建能解析依赖 —— 否则变异分数会凭空变好。26 个变异体逐条：`after()` 四列的 coalesce 各一条（4）、`after()` 顺手动 createdAt / 不动 updatedAt（2）、`ofNew` 状态写成 SUBMITTED / filledQty 写 null / 丢掉 limitPrice / createdAt 写 0（4）、`isOpen` 取反 / 恒真（2）、`save` 追加而不替换、`findOpen` 不过滤、`fills` 把活列表交出去、`saveFill` 丢单（4）、区间 from / to 改成排他（2）、四个 `save*` 各丢一条（4）、区间过滤整个删掉、按 ruleId 不过滤（2）、`InterceptionRecord.timestamp()` 取信号的时间戳、`ruleId()` 取 level（2）。 |
| 2026-09-10 06:02 | T310（业务库六张表 DDL `BusinessSchema` + `JdbcOrderStore`） | 通过：新增 `BusinessSchema`（六张表一份 DDL）、`JdbcOrderStore`、`JdbcOrderStoreTest`（11 用例，临时 SQLite 文件），`mvn -B -o clean verify` 全绿：alpha-app 79→**90**，全仓 477→**488**，其余模块不动。变异测试 **23 个：22 杀 / 1 存活 / 0 无效**（`/tmp/mutate_t310.py`），存活体另做了一次**双变异实验**（`/tmp/mutate_t310_pair.py`）定性，见下。**六张表放一个类、谁先被构造谁建全部**：一个库一份 schema，取舍 15 把表数钉在六，`IF NOT EXISTS` 让 `JdbcOrderStore` 与 T311 的 `JdbcRecordStore` 构造顺序无关、重启是 no-op；拆成两半的结果是四张表住一个文件两张表住另一个，加一列的那天两个文件对「这个库是什么」各说各话。**方言中立付出三笔具体代价**：①不用保留字（`order_status` 不是 `status`、`rule_level` 不是 `level`，同 `klines.interval_code` 躲 `interval` 的理由）；②不用方言 upsert —— 先 `SELECT seq` 再决定 INSERT 还是 UPDATE，因为 MySQL 报 changed 而 SQLite 报 matched，两个驱动的 `executeUpdate()` 都答不了「这行是不是新的」（沿用 `JdbcKlineRepository` 已经踩过的结论）；③不用自增，`AUTOINCREMENT` 与 `AUTO_INCREMENT` 拼法分裂。**本任务真正的难点是：契约承诺的顺序，SQL 给不出来。** `findOpen()` 要「首次保存序」、`fills(id)` 要「保存序」，而三个候选列全都不行 —— `created_at` 在同一根 bar 开两张单时打平；`business_ts` 在同一张单的部分成交上打平；`event_id` 是**构造**时分配的，所以一个后到的旧成交（WebSocket 漏了、REST 补回来）构造晚 id 反而大，按 id 排会把它排到后面去。于是两张表各带一个由 store 分配的 `seq`，它是唯一含义为「这套系统按什么顺序被告知」的列。三个变异体逐个钉死：`find-open-orders-by-created-at`、`find-open-orders-by-updated-at`、`fills-order-by-event-id` 与 `fills-order-by-business-ts`（后两个由 `fillsStayWithTheirOwnOrderInTheOrderTheyWereStored` 杀掉 —— 那个用例刻意让**较早的成交最后入库**）。**`seq` 用事务内 `MAX(seq)+1`，不缓存进字段**：缓存计数器会让同一文件上的两个实例发出同一个号，第二个 INSERT 撞唯一约束，而报错信息只说约束、不说原因；每次读一次索引换掉这个坑，顺带让「重启」「实例个数」都不成为前提。用例 `twoLiveStoresOverOneFileDoNotClaimTheSamePositionTwice` 钉这条（生产里只有一个 bean，但缓存版的失败现象太难认，不值得省这一次查询）。**UPDATE 刻意不写 `seq` 也不写 `created_at`**：这就是「替换这一行、不挪它的位置」的 SQL 形式 —— 迁移改的是这张单是什么，不是它什么时候到的；`update-moves-the-row`（`SET seq = 999999`）与 `update-writes-created-at-as-updated-at` 两个变异体分别钉这两半。**`OPEN_STATUSES` 从 `OrderStatus.values()` 推导，不写死三个名字**：写死的 `IN` 列表在加第七个状态时照样编译，并把它当成已收市 —— 而新状态恰恰是对账（FR-EX-04）最该看见的那一张单；`open-statuses-ignores-the-enum` 与 `open-statuses-inverted` 两个变异体钉死过滤方向。**价格与数量存 `VARCHAR` + `toPlainString`**：把 `orders.qty` 改成 `DECIMAL(24,8)` 的变异体被两个用例杀掉（`0.1500` 回来变成 `0.15`，`isEqualTo` 对 BigDecimal 是 scale 敏感的），这就是取舍 5/15 的可执行版本；`decimal-read-rescales-to-eight` 再从读侧夹一次（统一 `setScale(8)` 同样杀）。`strength` 是六张表里唯一的 `DOUBLE`：它不是钱，两个方言的 8 字节浮点与产生它的领域模型是同一个 IEEE double，没有列类型可归一化的东西。**四个可空列写 SQL NULL、读 Java null**：列上 `DEFAULT 0` 或读侧 `COALESCE` 都会把「交易所什么都没说」变成「交易所说了 0」，`nullable-decimal-reads-as-zero` 被 `theColumnsTheExchangeSaidNothingAboutComeBackNullRatherThanZero` 杀掉。**唯一的存活体不是测试漏洞，而是一条只在运行期响的约束**：`schema-orders-seq-is-not-unique`（去掉 `orders.seq` 的 UNIQUE）。为判断它到底承不承重，做了双变异实验：同时去掉 UNIQUE **并**把 orders 的 seq 分配写死成 1（fills 的分配不动，免得它的 `PRIMARY KEY (seq)` 违规把答案盖住），结果 **11 个用例全绿** —— 没有任何读能发现「两行占了同一个位置」，列表只是变得含义不明。所以这条约束是唯一能把分配 bug 变成失败的东西，作用点在运行期而不在测试里；这句话已写进 `ORDERS` 的 javadoc，而不是记一笔「不可杀」就算完（`next-seq-is-always-one` 之所以被杀，杀的正是这条约束在响）。**一处自我修正**：`savingTheSameOrderAgainReplacesTheRowAndLeavesItWhereItWas` 原来两次保存都走同一个 helper，迁移后的 `updated_at` 恰好仍比第二张单小，于是「列表不按 `updated_at` 排」这条其实由 `anOrderSavedLaterWithAnEarlierCreatedAtStillListsLast` 一个用例在扛，而这个用例的 `.as()` 描述 over-claim 了。改成迁移发生在 `T0+9`（晚于第二张单，也正是实盘里的样子），并顺带断言 `created_at` 不动、`updated_at` 动、FILLED 之后 `avg_fill_price` 仍在；改完复跑三个受影响的变异体，`find-open-orders-by-updated-at` 现在由**两个**用例杀掉。**改动只加断言，故此前 22 个 kill 不受影响**，且双变异实验是拿改完的 11 个用例跑的。23 个变异体逐条：DDL 三条（`orders.qty`→`DECIMAL(24,8)`、六张表少建一张、`orders.seq` 去 UNIQUE）；列表顺序四条（`findOpen` 按 `created_at` / 按 `updated_at`、`fills` 按 `event_id` / 按 `business_ts`）；`fills` 不按单过滤；可空列读成 0；读侧统一 rescale 到 8 位；`symbol` 写成 `binance()` 而非 `unified()`；`status` 写成小写（钉死枚举名就是线格式）；INSERT 把 `updated_at` 当 `created_at`；UPDATE 把 `created_at` 当 `updated_at`；UPDATE 顺手把 `seq` 写成 999999；fill 的 `business_ts` 写 0；`saveFill` 不执行 `executeUpdate`；`nextSeq` 恒为 1；恒走 INSERT（撞主键）；恒走 UPDATE（一行都存不进去）；`OPEN_STATUSES` 不过滤 / 取反；`collect` 把活列表交出去。 |
| 2026-09-10 06:14 | T311（`JdbcRecordStore`：四张 append-only 历史表的读写 + `BusinessSchema` 补索引、strength 改文本） | 通过：新增 `JdbcRecordStore`、`JdbcRecordStoreTest`（13 用例），改 `BusinessSchema`（两个 `strength` 列 `DOUBLE`→`VARCHAR(32)` + 七个索引），`mvn -B -o clean verify` 全绿：alpha-app 90→**103**，全仓 488→**501**，其余模块不动。变异测试 **42 个：41 杀 / 1 存活 / 0 无效**（`/tmp/mutate_t311.py`；`/tmp/mutate_t311_strength.py` 可单跑五个 strength 相关变异体复查），另做**双变异实验**（`/tmp/mutate_t311_pair.py`）为一条断言定性。**本任务的核心发现是两次实测，它们推翻了我自己在 T310 写进出厂文档的一句论断。** T310 把 `strength` 留在 `DOUBLE`，理由是「它不是钱，两个方言的 8 字节 IEEE 值与产生它的领域模型是同一个 double，没有列类型可归一化的东西」—— 这句话是错的，而且错得很安静：实验 1（`/tmp/t311_double_check.py`，只把两列 revert 成 `DOUBLE`、绑定仍是 `setString`）只挂 **1** 个用例，且只挂在 `-0.0` 上（SQLite 的 REAL 亲和性把 `-0.0` 还成 `0.0`，正是取舍 5 要避免的归一化，出现在没人会想到去查的那一列），而 **NaN 活着** —— 文本绑定写进去的是 `"NaN"`，SQLite 转不成数字就把文本原样存进 REAL 亲和列，纯属巧合。实验 2（`/tmp/t311_binding_check.py`，列保持 `VARCHAR(32)`、只把两处绑定改成 `setDouble`）挂 **2** 个 —— NaN 到驱动时变成 NULL，被 `NOT NULL` 以 `SQLITE_CONSTRAINT_NOTNULL` 拒掉，`signals` 与 `risk_interceptions` 各一次。**所以列类型与绑定是两道独立的防线，而两半分别失效**；这值得明说，因为每一半单独看都像够用（改错一半只挂一个用例，另一半挂在另一个值上）。**这条是正确性不是洁癖**：NaN strength 恰恰是 `PositionSizer.RULE_STRENGTH` 用 `!(strength >= 0 && strength <= 1)` 拒掉的那个值，于是数值绑定会让**最值得审计的那条拦截记录写不进去**，并从闸门抛到引擎线程上（FR-RK-08 要的是「可查」，一个抛异常的闸门什么也没记）。**我先把错的机制写进了文档**：`BusinessSchema` 最初的 javadoc 说 `DOUBLE` 错了是因为「驱动把 `Double.NaN` 绑成 NULL，所以 `NOT NULL` 列拒绝它」—— 实验 1 证伪了这个机制对**列**成立，它属于**绑定**。修法是重写成两段、把「两半分别失效」与实测结果写进去，把每条测试的注释改成说清它钉的是哪一半，并在 `saveSignal` 的绑定处加一句指向 `BusinessSchema` 里它覆盖不到的那一半。**索引是补一句已经出厂的承诺**：`InMemoryRecordStore` 的 javadoc 早就写了「both the reason the JDBC implementation has indexes」，而 `BusinessSchema` 一个索引都没有。补七个，每个「读要过滤或 range 的列」一个（`orders.order_status` 因为 FR-EX-04 的对账定时器每 60s 调 `findOpen()`、`fills.client_order_id` 因为每次成交都按它查）。**刻意全不 UNIQUE**：同一根 bar 上的所有信号共享一个时间戳，这里一个 UNIQUE 索引会把一次正常运行变成约束失败（`schema-signals-index-is-unique` 变异体钉死）。**`business_ts` 上的 range 不能搭 `seq` 主键的车**，即使两者通常一致 —— 它们恰好在「事实迟到」时分叉，而那正是 FR-EX-04 存在要处理的情况；这条写进了 javadoc。**`seq` 沿用 T310 的同一套理由与算法**（事务内 `MAX(seq)+1`，不缓存进字段），tempting-but-wrong 的候选是 `business_ts`；四张历史表全是 append-only，**整个写路径只有 INSERT、根本没有 update 分支**，所以 `JdbcOrderStore.save` 那个「先 `SELECT seq` 再决定 INSERT/UPDATE」在这里没有对应物 —— 没有任何东西能改写历史，这句话本身就是「为什么它不是 orders 的第二份实现」的答案。**整个写路径收成一个 `append(what, table, insertSql, binding)`**：每次保存都是同样两步（分配 `seq`、让调用方绑第 2..n 列），第 1 列在这里绑，于是没有哪个 `save` 能忘记它或绑两次，调用方的下标一律从 2 开始（`seq-bound-at-the-wrong-index` 变异体钉这个约定）。`table` 与 `insertSql` 是两个参数而不是一个，因为 `seq` 是**按表**的，而一条语句无法被问出它写的是哪张表。**刻意不与 `JdbcOrderStore` 共享机制**（约 60 行：`nextSeq`/`read`/`write`/`setDecimal`/`decimal`/`collect`/`RowMapper`/`ConnectionWork`/`store`）。考虑过抽一个 `BusinessDatabase`，三条理由否掉：①`JdbcKlineRepository` 本来就自留一份 `decimal()`/`store()`，「按约定重复」是这个仓库既有的房规；②schema 永远闭在六张表两个 store，能证成抽象的第三个实例不可能出现；③T310 的 23 个变异体里有 3 个打的正是会被搬走的代码，抽了会让已经写进本文件、已经提交推送的 22/23 证据失效。**拦截行 19 列、三个嵌套类型反着重建**：写是 `InterceptionRecord → SignalFacts → SignalEvent`，读是 `SignalEvent → SignalFacts → RiskRejection → InterceptionRecord`（后两者的构造参数里嵌着前者）。两个时间戳差 1ms、信号在前闸门在后（内存版注释说的「策略先决定、闸门后看」在这里变成两个可失败的绑定），`interception-writes-signal-ts-as-the-gate-instant` 与 `interception-writes-gate-instant-as-signal-ts` 互换着钉；同一链上另有 `interception-writes-cash-into-equity`、`interception-writes-symbol-notional-into-signed-qty`、`interception-reads-price-from-total-notional`、`interception-reads-severity-from-the-level-column`、`interception-writes-level-in-lower-case`。**scale 断言比内存版更严**：`InMemoryRecordStoreTest` 的钱用的是 scale 不敏感的 `isEqualByComparingTo`，这里全部 `isEqualTo`（AssertJ 对 BigDecimal scale 敏感）—— 内存版今天平凡通过的那些断言，指向 NUMERIC/DECIMAL 列的那天会响亮地失败，取舍 5/15 的可执行版本就落在这里（`schema-interception-equity-is-decimal` 一个变异体被三个用例同时杀掉）。`scaledFacts()` 手搓而不从 `Portfolio` 推导，为的是让六个钱列以「任何账本都不会产生的 scale」进去，断言才是关于列、不是关于 `Portfolio` 的算术。**契约逐条照 `InMemoryRecordStoreTest` 重写**（它出厂 javadoc 明说「These are the assertions T311's JDBC store must also pass」），重复它是唯一能知道两个实现对同一个问题给出同一个答案的办法；SQL 独有的四条失效模式另立：插入序（SQL 里没有对应物 → `theFourReadsReportWhatWasSavedNotWhatTheTimestampsSay` 倒着存 `T0+5` 再 `T0+1`，五个读都必须返回那个顺序）、scale 要靠列类型活下来、strength 要**列与绑定都是文本**、ruleId 要精确相等。range 用例四张表各存 4 行（from-1/from/to/to+1），因为**四个 SQL 谓词是分别写的**，「between」是 off-by-one 最容易隐身的地方（这也是内存版把这条写成「冗余」并明说要写出来的理由）；五个变异体分别把四张表的两端改成排他、再加一个 `range-binds-from-and-to-swapped`。**一个测试漏洞由变异测试查出、而不是读代码查出**：`schema-strength-is-double-in-interceptions` **存活** —— 因为只有 `signals` 被 `-0.0` 探过，`risk_interceptions.strength` 可以自由改回 `DOUBLE` 而没有一个用例注意到。修法是让 `strengthSurvives…` 对七个值 `{1.0, 0.75, NaN, +Inf, -Inf, -0.0, MIN_VALUE}` 各存一条信号**和**一条拦截（14 行共用 `T0`，顺带证明顺序来自 `seq`、两个 `business_ts` 索引都不 UNIQUE），并把理由写进用例注释。复跑五个相关变异体 **5/5 全杀**；改动只加断言，故此前 40 个 kill 不受影响。**唯一的存活体不是漏洞**：`decimal-written-with-tostring`（`toPlainString`→`toString`）对这些列持有的每一个 scale 都产生同一个字符串，这个选择是关于「一个人用 SQL 客户端打开这个文件时看到 `0E-8` 还是 `0.00000000`」，不是关于值，没有测试能也不该杀它 —— 与 T310 记的是同一条解释。**双变异实验为一条断言定性**：`interceptions("RK-02")` 必须为空那条挂着 `.as("equality, not LIKE: a rule id is not a prefix search")`，但单改 `= ?`→`LIKE ?` 测不出来（绑定的值不带通配符时两者行为相同），所以两处一起改（`LIKE ?` + `ruleId + "%"`）：结果 **KILLED**，`interceptionsComeBackByRuleAndOnlyByThatRule:222` 挂在那条 `.as()` 上 —— 断言承重，不是装饰，保留。**唯一那个用例 `whicheverStoreArrivesFirstLeavesTheOtherACompleteDatabase` 是 T310 那句「构造顺序无关」的可执行版本**：从 `sqlite_master` 断言六张表名与七个 `idx_%` 索引名都在，再证明 `JdbcOrderStore` 能在同一个文件上 save/find/findOpen。**自检修掉五处**：未使用的常量 `SIGNAL_COLUMNS`；私有 `insertStatement(table)` switch 里那个**不可达**的 `default -> throw`（四个调用方传的都是本文件字面量，为不可能发生的情况做校验）→ 给 `append` 加 `insertSql` 参数、删掉 switch，并把「表名不能做绑定参数」那句 javadoc 挪回还在拼接的 `nextSeq` 上；一条**同义反复**的断言 `read.getFirst().eventId() == store.signals(T0,T0).getFirst().eventId()` → 改成真 round-trip，而第一版修法也是错的（新构造的 `SignalEvent` 会经 `EventIds.next()` 分配**新** eventId，永远不等于库里那条），最终改成保存前先捕获两个对象再 `containsExactly(first, second)`；`setDecimal` 的单行 javadoc 排版；`append` 改签名后 lambda 体缩进不齐。**两个环境坑（都出在探针上）**：`java` 单文件源码模式跑 SQLite 要①直接构造 `org.sqlite.SQLiteDataSource`，因为 `DriverManager` 看不见那个 classloader（`No suitable driver found`）②把 `slf4j-api-2.0.17.jar` 放进 `-cp`，因为 sqlite-jdbc 的 `JDBC.<clinit>` 要它。探针 `/tmp/t311-probe/Probe.java` 是上面两次实测证据的来源。42 个变异体逐条：strength 两列各改 `DOUBLE`（2）、两处绑定各改 `setDouble`（2）；钱列改 `DECIMAL(24,8)` 四处 + 读侧统一 rescale 到 8 位 + `toPlainString`→`toString`（6）；六张表少建一张、七个索引少建一个、`idx_signals_business_ts` 改 UNIQUE、构造时不建 schema（4）；五个 `ORDER BY seq`→`ORDER BY business_ts`、`nextSeq` 恒为 1、`seq` 绑在下标 1（7）；五处 range 端点改排他（signals 两端、equity 上端、positions 下端、interceptions 上端）+ from/to 互换（6）；`interceptions(ruleId)` 过滤错列（1）；拦截行写读串列七处（`signal_ts`↔`business_ts` 互换 2、`cash`→`equity`、`symbol_notional`→`signed_qty`、读 `price` 从 `total_notional`、读 `severity` 从 `rule_level`、`level` 写小写）；`signal` 从 `event_id` 读时间戳、拦截里的信号读闸门瞬时、`position` 交换 `entry_price` 与 `mark_price`、`direction` 写死、`symbol` 写成 `binance()` 而非 `unified()`（5）；`append` 不执行 `executeUpdate`、`collect` 把活列表交出去（2）。 |
| 2026-09-10 06:52 | T312（OMS 订单状态机：`OrderManager` 替换占位 + 新增 `OrderReportEvent`，见取舍 19） | 通过：新增 18 个单测（execution 15→28、common 27→30、engine 12→13；全仓 501→518 全绿）。状态机写成**一张表而不是一串 if**（`LEGAL: Map<OrderStatus, Set<OrderStatus>>`），于是「哪些迁移合法」只有一个答案、且能被用例穷举；表的四条规则各带理由写在注释里：①**允许向前跳、绝不允许向后退** —— NEW→FILLED 看着像漏了一步，但它正是「重启后既漏了 ack 又漏了成交」时对账会报的事实；拒绝它会让行里写着 NEW 而交易所已经动了仓位（FR-EX-04 要防的正是这一种背离），而在中间凭空补一个 SUBMITTED 是写下一条从未发生过的事实。②三个终态**没有任何后继**（验收标准的「终态不可再迁移」）。③PARTIALLY_FILLED→PARTIALLY_FILLED 是唯一自环（分笔成交会多次报同一状态，拒绝第二笔就把余量搁死）。④**PARTIALLY_FILLED→REJECTED 刻意拒绝**：REJECTED 意味着交易所从未接受该单、什么都没成交，已成交一部分之后余量应是 CANCELED；报成拒单会把「钱已经动了」抹掉，而且抹在人最先读的那一列 `statusMessage` 上。**拒绝一律是事件不是异常**：重连流里的重复报告、迟到 ack、对账修正在实盘都是日常，抛异常会落进 `EventEngine.dispatch` 的 catch —— 记一条日志然后继续，行没变、下游谁也不知道；所以 OMS 保持原行、不发关于该订单的任何事件、改在系统已有的告警通道上说清楚（`EX-refused-transition` / `EX-unknown-order`）。**严重度按「有没有钱 involved」分**：被拒的成交 CRITICAL（有一笔行里不会反映的执行，账本与交易所从此对不上仓位），被拒的生命周期报告 WARNING（通常只是交易所比我们慢，行才是更好的答案，对账是把它升级的后手）。**先写库再发事件**，open 与 onReport 两条路径都如此 —— 能在公告之前看不到状态的观察者，其行为取决于 handler 注册顺序，这与取舍 16 对「先写账本再发 `FillEvent`」是同一条规则、同一个理由。**成交后的累计量与均价由 OMS 自己算**而不是取报文里的：报文带的是本次成交，行才知道之前那几笔；均价用与 `Portfolio` 开仓均价同一个加权式（`Money.divide`），所以订单行与它成交进去的账本不可能取整不同。**边界刻意不越**：不持有 gateway、不持有 clock、不写 `Portfolio`、不发 `FillEvent`、不落 fills —— 发送路径（含「交易所拒单」与「交易所不可达」之分）是 T313，先写账本再发成交是 T314，于是这些缺口是**结构性的而不是注释性的**。**写测试时查出两个真实缺陷**：①`isTrade()` 是 Jackson 的 bean 风格 getter，会被当成名为 `trade` 的属性写进 journal，而 record 只能按规范构造器还原，读回来直接 `UnrecognizedPropertyException` —— 这个事件**写得进去读不回来**，而读不回 journal 的崩溃恢复等于恢复不了它本来要恢复的订单（加 `@JsonIgnore`，理由写在方法上）；②初版 `Attempt.REJECT` 造的报文带着 `exchangeOrderId`，断言却写着「被拒的订单从未拿到过 id」并断言它等于那个 id —— 自相矛盾，改为报文带 `null`（真实拒单没有 id）+ 断言 `isNull()`。**变异测试：79 个变异体全部杀掉，0 存活、0 编译不过**：合法迁移表逐条增删（14，含三个终态各自复活、NEW/SUBMITTED 放开成 `allOf`、PARTIALLY_FILLED 的自环删掉与放开 REJECTED/SUBMITTED）；合法性判定（3：反转、转置、只问自己）；成交算术（14：累计量取本次/不动、加权均价的四个操作数各错一处、除以订单量、除以价格、比较方向反转与取等、两个状态互换、`after` 参数转置、丢交易所 id、不推进 `updatedAt`）；生命周期分支（4：丢拒因、丢 id、不推进时间、把已成交量清零）；fill-claim 守卫（5）；写库与发事件的顺序（6：两条路径各自「先发后写」「只发不写」「只写不发」）；公告字段（4）；拒绝与告警（11：严重度互换/恒定 CRITICAL/恒定 WARNING、规则号互换、详情里两个状态写反、丢拒因、未知订单守卫反转/删除/整条告警删掉/升级）；入口与分发（4）；`OrderReportEvent`（14：两个工厂的 id 转置、量价互换、价费互换、丢 fee/message/业务时间、凭空造 status 与成交量、`isTrade` 三种改判据、去掉 `@JsonIgnore`）。**首轮 8 个存活体全是断言缺口而非等价变异体**，逐个补死后重跑全量 79 个：①没有任何 attempt 以 NEW 为目标 → 加 `RESTATE_NEW`（重连流的重复报告可以带一个更早的状态，机器必须拒绝被告知「这单还是新的」）；②成交路径从不被断言记录了 `exchangeOrderId`（NEW→FILLED 前跳时那次成交可能是关于该订单听到的第一件事，它的 id 是对账唯一的把手）；③fill-claim 守卫只被 FILLED 试过 → 两个 fill 状态都试（只拒 FILLED 会让一条声称 PARTIALLY_FILLED 的报告造出「成交了一部分」而其实什么都没成交的行）；④「先写库再发事件」只覆盖了报告路径，open 路径没覆盖（改为同一个 publisher 依次驱动 open 与 ack，断言 `[NEW, SUBMITTED]`）；⑤⑥公告的 `avgPrice`/`timestamp` 从未与行比对（引用订单数量或创建时间的公告依然是格式完好的 `OrderUpdateEvent`）→ 穷举用例改为**逐字段与行比对**；⑦详情把两个状态写反仍能通过 `contains("FILLED")`+`contains("SUBMITTED")` → 钉住「is FILLED」「cannot become SUBMITTED」的顺序；⑧`isTrade()` 加一个 `fee != null` 判据杀不死（所有用例的成交都带 fee）→ 补一条 commission 字段没到的成交。**顺带修掉一处约定破坏**：`OrderReportEventTest` 起初按生产包放在 `common/event/` 目录，与本模块既有 6 个测试的扁平约定（一律 `package …common;` + import 生产类）不一致，改回扁平包；同时按取舍 19 把 T317 的任务文字从「解析 → `FillEvent`/`OrderUpdateEvent`」改成「解析 → 发 `OrderReportEvent`」，否则流解析会去发布 OMS 自己的出口事实。 |
| 2026-09-10 07:20 | T313（下单出口：`OrderOutbox` 接缝 + `OrderSender` 工作线程 + 边界 6「交易所没回答」，见取舍 20） | 通过：新增 `OrderOutbox`/`OrderSender`/`OrderSenderTest`（13）+ gateway 侧 `ExchangeUnreachableException`，改 `OrderManager`/`OrderManagerTest`（15）/`ExchangeGateway` javadoc；`mvn -B -o clean verify` **exit 0 BUILD SUCCESS**（common 30 / engine 13 / gateway 22(1 skip) / strategy 61 / risk 135 / execution 28→**43** / backtest 126 / app 103(1 skip) = 全仓 518→**533** 全绿）。**为什么要多一个接缝而不是让 `OrderManager` 自己发**：`placeOrder` 是阻塞 REST，而 `EventHandler` 的契约明写「run ON the single engine thread … never block, never do network IO here」，在 `onEvent` 里发单会把交易所的抖动变成整条流水线的心跳。于是 `OrderManager.open()` 的顺序钉成 **行 → 公告 → 交出**：行必须先存在，否则任何关于它的回答都无处落（用例 `theRowExistsBeforeTheOrderIsHandedOverForSending` 让 outbox 在收到请求的当下回查 `store`，断言它读到的是 `NEW`）；交出放在最后，且 `submit` 的返回值刻意不读——`OrderOutbox` 的 javadoc 已写明调用方「must not respond to it by sending the order some other way」，读了它就等于给「被拒绝」留了第二条发送路径。**发送线程回来的结果走 `EventEngine.publish` 而不是 `EventPublisher`**：后者是 handler 级联出口（javadoc：「The only way a handler emits follow-up events」），`publish` 才是「thread-safe entry point for external producers (gateways, feeders, REST)」；用错那个会把跨线程的写入伪装成同轮级联。**取舍 20 把 §6.6 的「后者重试而非判定失败」拆成三处责任**：①**区分**放在网关边界、并且是一个**类型**而不是错误码——`OrderAck` 永远是「回答」（拒绝也是回答，所以它变成 `OrderReportEvent` 而不是告警，用例 `aRejectedPlacementIsAnAnswerSoItBecomesAReportRatherThanAnAlert`），`ExchangeUnreachableException` 是「没有回答」，且网关**绝不用空值/默认值报告「没回答」**（一次故障中返回空挂单列表会让对账把本地状态「纠正」成虚构）；②**响应**放在 `OrderSender`：CRITICAL 告警 + 行原地不动（`NEW`）+ 由对账裁决，两种成因（交易所不可达 / 自己的发送路径抛了）用两个 ruleId（`EX-exchange-unreachable` / `EX-send-failed`）因为修法与责任方不同，但**结论只写在一处** `unknownOutcome`——否则两条 detail 会各自漂走，用例 `ourOwnSendPathFailingIsAlsoUnknownButUnderItsOwnRuleId` 就是在设计变异体时发现这条漂移才补的断言；③**重试归网关（T316）不归 sender**：只有握着连接的那一层知道请求有没有上线，而重发一个首次已落地的 `clientOrderId`，回来的是对一笔**活着**的订单的**拒绝**，与真拒绝不可区分，于是策略再发一次信号、交易所手上就有两笔。代价写进了取舍 20：不可达的单会挂到下一次对账（默认 60s，T318）——用一次可恢复的状态分歧换「绝不重复下单」，因为后者是真金白银。**`ExchangeUnreachableException` 是无检异常**，理由不是省事：`EventHandler` 没有 `throws` 子句、对账在引擎线程的定时器上够到交易所，有检异常会在每个这类调用点被包一层，而被 `catch` 的正是那个包装类型——于是「有没有回答」这个区分会活过编译器、活不过代码。**幂等**：`handedOver` 是引擎线程限定的 `Set<String>`（`submit` 只在 `onEvent` 里被调），**从不清空**，因为 journal 重放会带同一个 `clientOrderId` 再来一次（`aRedeliveredOrderRequestLeavesTheRowExactlyAsItWas` 现在故意传**另一个**同 id 的实例，这才是重放的真实形状，也证明拒绝是按 id 而不是按对象身份）；真要重下是**新订单新 id**（`ClientOrderIds`），不是复活旧的。**测试里踩到并修掉的两个真实缺陷**：①`null` ack 的解读原先在两个 `catch` 之外，一个返回 null 的网关（是 bug，但是可能的 bug）会让 NPE 逃到 `runLoop` 的兜底 catch，于是**这次尝试一个事件都没有**，违反类不变量「exactly one event per attempt」——把 ack 解读移进 `try`，用例 `aGatewayThatAnswersWithNothingStillProducesExactlyOneEvent` 钉住；②`request()` 每次都调 `EventIds.next()`，所以两次调用产出的 record **永不相等**，`containsExactly(request())` 在两条用例上假失败（eventId 268 vs 273）——加 `harness(OrderRequestEvent)` 断言「交到 outbox 的就是这一个对象」。`anOutcomeIsStampedWithWhenTheAttemptEndedNotWhenTheOrderWasRaised` 钉住时间戳语义（用 `VirtualClock` 推进，告警带的是尝试结束时刻）。**变异测试 33 个：32 杀 / 1 存活 / 0 无效**，源文件按 sha256 校验还原、还原后全量 verify 仍绿。存活的那个是 `close_never_interrupts`，**诚实解释而不粉饰**：worker 停在有界的 100ms poll 上，无论有没有中断都会醒来看见 `running == false` 并退出，中断只是缩短等待；它真正的价值（中止一个在飞的可中断 REST 调用）依赖 T316 才存在的网关实现，现在测等于在断言我的桩的中断行为而不是 sender 的。保留中断。脚本本身也按纪律跑：变异体不编译算 INVALID 不算 KILLED（本次 0 个），锚点先做唯一性断言（`ACCEPTED_TS` 第一版按分号结尾、源码那行其实是三元表达式的一部分、无分号，被断言在跑之前拦下）。**`SharedOrderStore` 是带 `synchronized` 的测试装饰器**，正当性来自 `OrderStore` 的线程契约原文：「Written on the event-engine thread only … An implementation that must also serve reads from another thread owns that synchronization; the contract deliberately does not promise it」——`OrderSenderTest` 里 worker 线程写、测试线程读，正是契约说「自己负责」的那种实现。**`OrderOutbox` 顺手多写了一段 javadoc 约束重放**：恢复要重放 journal，而 journal 里每笔订单的**交易所回答**都已经在里面了，所以接一个会发送的 outbox 重放等于把活单再下一遍；接一个不发送的 outbox 重建同样的状态而完全不碰交易所，且 OMS 一行不用改。**给 T319 留的硬约束**：alpha-execution 无 Spring（红线扫描盯着编译后的常量池），所以装配必须用 `@Bean(initMethod = "start", destroyMethod = "close")`——`OrderSender` 的 worker 是 daemon 但 `EventEngine` 的循环线程刻意不是（「the engine IS the application's reason to stay alive」），漏掉 `destroyMethod` 会让测试 fork 挂住，`OrderSenderTest.shutDownEverything` 就是为此按逆序 close 并等 worker 真的消失（只数**存活**线程，空答案才意味着结束而不只是停止）。**日志行不可断言**：父 pom 的测试依赖只有 junit-jupiter + assertj-core，没有 logback，所以 `close()` 对未发送订单的那条报告只能靠行为验证（`closingASenderThatNeverStartedIsHarmless` + 队列排空用例），不能断言文本。**同时挂出两条补漏任务**：`RecordStore` 至今没有任何生产代码调用它的写方法，而 `RiskGate.block()` 只发告警不落 `InterceptionRecord`，`SignalFacts` 又只存在于一次 `onEvent` 调用内部、从不发布，任何 handler 注册顺序都还原不出它 → **T313b**（把 `RecordStore` 注入 `RiskGate`，在 `block()` 里写行，T320 依赖它）；`equity_snapshot`/`positions` 是六张表里唯一需要**采样**、而不是总线上已有事实的两张 → **T318b**（采样器，持仓行必须带当时的 `markPrice`，否则「用后来的标记价重估过去的持仓」会变成可能）。两条都已并入 P3 出口标准，防止被跳过。 |
| 2026-09-10 07:52 | T314（成交回报处理：OMS **先写 `Portfolio` 再 publish `FillEvent`**（取舍 16）+ 落库） | 通过：`OrderManager` 加第三个构造参数 `Portfolio`，`onReport` 按 `isTrade()` 分成 `onLifecycle` / `onTrade`，新增 `RULE_MALFORMED_TRADE`；`OrderManagerTest` 15→**30**、`OrderSenderTest` 的 `Running` 多带一个 `Portfolio`（它注册真 OMS 对同一个引擎，是唯一把环闭上的地方）；`mvn -B -o clean verify` **exit 0 BUILD SUCCESS**（common 30 / engine 13 / gateway 22(1 skip) / strategy 61 / risk 135 / execution 43→**58** / backtest 126 / app 103(1 skip) = 全仓 533→**548** 全绿）。**取舍 16 在这一任务落地**：OMS 在 paper/live 占的正是回测里 `SimulatedExecutor` 的那个位置 —— 谁确知成交发生，谁就在 publish `FillEvent` 之前把成交写进 `Portfolio`。这是**跨模块契约而不是本地偏好**：`CircuitBreaker.onFill` 在事件到达的那一刻读 `portfolio.realizedPnl()`，把「与上一笔的差」当成本笔的结果，所以事后才更新的账本会让每一笔平仓都读成 0，连亏计数永远断不掉。**四次写入的顺序是 fill 行 → order 行 → 账本 → `FillEvent` → `OrderUpdateEvent`**，每一处都有理由：持久写在内存写之前，因为能失败的是持久的那两个，失败之后这一笔报文整笔未生效而不是半生效（行动了而账本没动，是只有对账才找得出的背离）；append-only 的 fill 行在 order 行之前，因为失败之后行还开着、`findOpen()` 还会返回它，对账会再来看 —— **反过来留下的残渣更坏**：行已经写着 FILLED，而 FILLED 是终态、没有后继，`findOpen()` 永远不会再返回它，于是账本里没有仓位、fill 表里没有记录、行却是终态，这是一处**永久静默**的背离。**这个顺序是写「故意让 store 写失败」的用例时、在跑之前就查出的真实缺陷**（初版是 `save` 再 `saveFill`）。账本在 `FillEvent` 之前（上条）；`FillEvent` 在 `OrderUpdateEvent` 之前，因为前者是因、后者是果，而这是 journal 读者需要的顺序。**三次写入的顺序只有在其一失败时才可观察**，否则它只是一段注释 —— 所以有 `FailingOrderStore`（带 `armed` 开关的装饰器，`onOrderRow()` / `onFillRow()` 两种），两条用例分别断言：抛异常之后行仍是 `NEW`、账本仍是平的、现金等于初始净值、一个事件都没发；fill 行失败那条还断言 `store.fills(ID)` 为空，order 行失败那条则断言 fill 行**在**（那个刻意的孤儿）且 `findOpen()` 仍返回这一行。**`armed` 是必需的**：不加它，`save` 会在 `open()` 里就抛，行根本不存在，用例量到的是「未知订单」路径而不是写失败。**规范刻度**：`price`/`qty`/`fee` 一进 `onTrade` 就各过一次 `Money.of`，于是 fill 行、order 行、账本三处拿到的是**同一串比特**，而不是三个「比较起来相等」的数 —— 首笔成交时账本的 entryPrice 就等于行里报的那个价。authority 是 `Money` 自己的 javadoc（「a fixed scale of 8 for stored values」），而取舍 5/15 把这些列存成 TEXT，`JdbcOrderStore`/`JdbcRecordStore` 写的都是 `toPlainString()`，TEXT 列给什么刻度就存什么刻度。行的 `avgFillPrice` 同理（`Money.of(Money.divide(...))`）。AssertJ 对 BigDecimal 的 `isEqualTo` 是刻度敏感的，这才让「规范刻度」本身可断言。**手续费缺失不等于成交缺失**：`report.fee() == null ? Money.zero() : Money.of(report.fee())`，0 同时进落库的 fill 和账本，于是两者彼此一致、只与交易所有差 —— 那是对账找得出的差；一边 null 一边 0 则找不出（且 `Portfolio.applyFill` 的 `money(fee)` 会 NPE）。**symbol 与 side 取自行而不是报文**，因为成交报文两者都不带（`OrderReportEvent.ofTrade` 没有这两个字段）；这也正是「本进程从未持有的订单」永远够不到账本的原因 —— 没有行，就没有任何东西能说出动的是哪个合约、朝哪个方向。**`RULE_MALFORMED_TRADE` 修掉 T312 留下的两个真实缺陷**：①对一张从未成交的订单发一笔 0 量成交，`afterTrade` 里 `Money.divide(x, ZERO)` 是**除零** → `ArithmeticException`，被 `EventEngine.dispatch` 的 catch 吞掉（记一条日志然后继续），行没变、下游谁也不知道；②对一张 PARTIALLY_FILLED 的订单发 0 量成交，会**被接受** —— 走的是 T312 刻意留下的那条唯一合法自环，于是公告了一次什么都没改的迁移。而 T314 之后抛异常比之前更坏：`Portfolio.applyFill` 自己就拒绝非正数量，异常若在行写完之后才抛出，就留下一行声称成交过、账本却从未见过的订单。所以守卫在**任何计算之前**跑，写库、写账本、发事件一律不做，只在系统已有的告警通道上说清楚（CRITICAL，detail 带订单号、「nothing was written」、「reconciliation decides」）。它与 `RULE_REFUSED_TRANSITION` 分开，因为两者成因与修法不同：那一条说的是「格式完好的报文在错误的时刻到了」，这一条说的是「报文本身不可信」。**成交不设标记价**（`aFillDoesNotSetAMarkPrice`）：回测里给账本打标记价的是 `SimulatedExecutor.onKline`（`portfolio.mark(symbol, bar.close())`），标记价是市场的意见、走自己的通道；用成交价打标会把「最后一笔成交价」变成对所有持仓的估值（paper/live 侧的标记价来自 T317 的流与 T318b 的采样器）。**一处已知隐患刻意不在本任务修，而是交给 T317 并改写它的任务行**：`OrderReportEvent` 不带交易所成交 id，所以重连流重投的成交报文与真正的第二笔在 OMS 眼里完全相同（PARTIALLY_FILLED 自环合法），会重复计钱；这个字段属于看得见 Binance `t` 的那一层，放在 OMS 里凭空造一个只会得到一个没人填得对的参数。终态那侧已经安全（FILLED 无后继，`aDuplicatedTradeOnAFilledOrderMovesNeitherTheRowNorTheBook` 钉住），漏的正是自环这一侧 —— 已把「必须带成交 id + 按 `(clientOrderId, tradeId)` 去重 + 对应单测」写进 T317 行，而不是只留在提交信息里。**自己写错的两次，都已修**：①`afterTrade` 的形参顺序写成 `(row, report, qty, price)` 而调用处按 `(…, price, qty)` 传 —— **两个相邻同型参数把互换藏住了**，8 条用例失败（`expected: 3.00000000 but was: 100.00000000` 之类）。修法不是把调用处改回去，而是把签名改成 **price 在 qty 之前**，与 `Portfolio.applyFill(symbol, side, price, qty, fee)` 和 `FillEvent.of(..., price, qty, fee, ts)` 同序，于是三处读起来一样、要错就同时错三处；变异体 `BOOK_PRICE_QTY_SWAPPED` 就是为此存在的。②`FailingOrderStore` 初版没有 `armed`（见上）。**变异测试 32 个：32 杀 / 0 存活 / 0 编译不过**，源文件按 sha256 校验还原、还原后全量 verify 仍绿。逐条：写入顺序（6：账本挪到 `FillEvent` 之后、fill 行与 order 行互换、两个 publish 互换、fill 行不落、`FillEvent` 不发、成交路径不发公告）；账本（4：整个不写、量价互换、成交顺手打标记价、side 恒为 SELL）；规范刻度（5：price/qty/fee/均价各自不归一、fee 的 null 不兜底）；行的算术（6：均价除以本次量、名义不乘量、均价忽略之前几笔、部分/全额判定取等、恒为 FILLED、丢交易所 id）；守卫（8：量守卫删除/取 `<0`、价守卫删除/只查 null/取 `<0`、守卫之后照写不误、严重度降级、换成 `RULE_REFUSED_TRANSITION`）；分发与状态机（3：`isTrade()` 取反、非法成交不拒、非法成交严重度降级）。**首轮 1 个存活体 `AVERAGE_NOT_NORMALIZED`，是断言缺口不是等价变异体**：既有用例的加权均价都在第 8 位小数内整除（100/1、300/2、600/3），`Money.of` 因此是空操作。补 `theStoredAverageIsRoundedToTheScaleThisSystemStoresMoneyAt`：3 张 @100 + 1 张 @100.00000001，累计名义 400.00000001 除以 4 = **100.0000000025**，不整除；断言行的均价刻度恰好是 `Money.SCALE` 且四舍五入到 100.00000000、公告带的是行里那个数而不是账本里更长的那个、账本仍是 100.0000000025（`Portfolio` 归一 cash / 已实现盈亏 / 手续费，**但不归一 entryPrice**）、两者之差小于第 8 位小数的半个单位。复跑确认已杀。**这条用例同时逼出一处诚实性修正**：`afterTrade` 的 javadoc 原先写「订单行与账本不可能取整不同」，而它只在整除时成立 —— 已改成写明分歧、它的界、以及为什么这个界可以接受（要两边比特一致就得改 `Portfolio` 的 entryPrice，那是所有回测数字所依赖的 P2 算术，不归本任务动）；同时改掉两条已经过时的用例注释（「scale 是 `Money.divide` 的」、「值不可能不同」）。**没有新增模块边**：`Portfolio` 住在 alpha-common，alpha-execution 的依赖红线扫描（`DependencyRedLineTest`）仍绿。**给 T318b 的一条约束**：`positions` 表要落 `entryPrice`，而 `JdbcRecordStore` 直接写 `position.entryPrice().toPlainString()` 且 `Portfolio` 不归一它，所以采样器落行时必须自己过 `Money.of` —— 否则 24 位有效数字会原样进 TEXT 列。 |
| 2026-09-10 07:58 | T313b（补 T309 的装配硬约束：`RiskGate` 注入 `RecordStore`，在 `block()` 里写 `InterceptionRecord`） | 通过：`RiskGate` 加第六个构造参数 `RecordStore records`，`RiskGateTest` 14→**22**、`RiskInterceptionMatrixTest` 逐行加断言（用例数 10 不变）；risk 135→**143**，全仓 548→**556**；`mvn -B -o clean verify` **exit 0 BUILD SUCCESS** 两次（改完一次、变异还原后一次）。**为什么是「行在告警之前」而不是反过来**：FR-RK-08 要的是「拦截记录可查（含命中规则与当时账户状态）」，而**唯一能指望它去回查的消费者，正是对告警作出反应的那一个**；顺序反了，它查到的就是空表。用例 `theRecordIsWrittenBeforeTheAlertThatAnnouncesIt` 因此**从 publisher 内部回查**、而不是等调用返回后再查——调用返回后两种顺序看起来一模一样，而决定成败的恰恰是顺序（断言 `rowsVisibleWhenTheAlertArrived` 恰好是 `[1]`）。**为什么由闸门自己写、不挂总线观察者**：`SignalFacts` 只存在于一次 `onEvent` 调用内部、从不发布，任何 handler 注册顺序都还原不出它，而它正是这条记录里唯一值钱的那一半（另一半 `RiskRejection` 在告警文本里已经有了）。**顺带修掉一个真实的一致性缺陷**：告警原先用 `clock.nowMillis()` 现取时间戳、而行用的是快照的时刻——回测里 `VirtualClock` 不动，两者恒等，缺陷完全隐形；live 里时钟是墙上钟，管道跑的时候时间在走，于是**告警与解释它的那条行会各说一个时刻**，而按时间窗查拦截（`interceptions(from,to)`）正是靠这两者配对。改成 `facts.nowMillis()`，用例 `theAlertAndTheRowCarryTheSameInstantEvenWhenAClockMovesMidDecision` 用一个**在被咨询时把钟推进 750ms 的规则**钉住（先断言钟真的动了，否则整条用例是空转）。`publishOrder` 里那次 `clock.nowMillis()` **刻意保留不动**：那里没有快照可复用，`clientOrderId` 与订单时间戳本就该是发送时刻。**快照上移到第一步之前**：原先 `SignalFacts` 在 trading-rules 查找之后才取，于是「规则缺失」这条 CRITICAL 硬停**根本没有账户可记**——而它拒的理由（没缓存精度）恰恰与账户无关，一条背后没有账户的拒绝回答不了「当时那么拒对不对」。上移之后四条 block 路径共用同一份快照，`theHardStopRecordsTheAccountItRefusedAgainst` 钉住硬停也带 equity/price/signedQty。**回测拿 `InMemoryRecordStore`**：`RecordStore` 是必填的第六参而不是可空的——可空的 store 会让 block 路径有两种形状，而只有一种被测到；`InMemoryRecordStore` 的 javadoc 写的正是「a mode that runs without a business database is given」这个用途。**没有选更小的丢弃式 sink**：那样写路径在 risk 自己的测试之外就完全不可观测，而一条已经存在的行，以后要被上层读出来时不必再碰闸门。**变异测试 13 个：13 杀 / 0 存活 / 0 无效**，源文件按 sha256 校验还原、还原后全量 verify 仍绿、`git diff` 复查无变异体残留。覆盖：行不写、行写在告警之后、告警现取时钟、行用重读的账本、硬停不落行、NoTrade 也落行、管道/sizer/`OrderFacts` 各自重取快照、`signalsBlocked` 不自增、SIZING 拒绝被归档到 ORDER、硬停被归档到 ACCOUNT、告警文本丢掉 strategyId。**其中一个变异体逼出一条断言补漏**：`SNAPSHOT_RETAKEN_FOR_SIZER`（把 sizer 的入参换成一次重读）**原本会存活**——`bothStagesAndTheSizerSeeOneSnapshotOfTheAccount` 名字里有 sizer，断言里却只比了两个管道阶段是不是同一个 `SignalFacts` 对象，而重读在同一个标记价下算出的数量完全相同。补法是加一个 `BookMovingRule`：**在被咨询时把标记价从 100 改成 200、且不作反对**（反对会在 sizer 跑之前就结束这次尝试，数量便无从观测），于是「1 万权益的 30% 按快照价 100 = 30」与「按重读价 200 = 15」区分得开；断言 `qty == 30.000` 杀掉变异体，也让这条用例的名字第一次名副其实。**一个刻意没有做成变异体的点，诚实记下**：把快照移回 trading-rules 查找之后、并让硬停自己内联构造一份 `SignalFacts`——这个变体在硬停路径上**行为等价**（同一时刻、同一账本、告警与行仍然配对），任何测试都区分不出，所以我没有伪造一个必死的变异体来充数。上移真正的价值是「一次决策只读一次账本」这条不变量本身，它由上面三个 RETAKEN 变异体分别钉住。**同时挂出 T313c**：grep 确认 `RecordStore.saveSignal` 在生产代码里**零调用者**——T313b 补上了 `saveInterception`，`saveEquitySnapshot`/`savePosition` 归 T318b，而 `signals` 表至今没有任何写入方，T321 的「每笔信号可在库中查询」会落空。T313c 的做法**与 T313b 相反、必须挂总线**：信号是总线事实而不是某次调用的内部快照，挂观察者才连风控从未看到的信号也一并记下（`StrategyEngine` 发出、却没走到闸门的那种正是复盘时最想查的）；写进闸门只会记下走到闸门的那些，把「策略发了什么」偷换成「风控收到了什么」。已并入 P3 出口标准。 |
| 2026-09-10 08:10 | T313c（`signals` 表的写入方：`SignalRecorder implements EventHandler`，挂总线） | 通过：新增 `SignalRecorder`（alpha-risk）+ `SignalRecorderTest`（8 用例），`BacktestRunner` 把 `InMemoryRecordStore` 从 `RiskGate` 的实参提成局部变量、与 recorder 共用一个 store，并把 recorder 注册在闸门**之前**；risk 143→**151**，全仓 556→**564**；`mvn -B -o clean verify` **exit 0 BUILD SUCCESS** 两次（改完一次、变异还原后一次）。**先改了自己写错的任务行，再动手实现**：T313c 原来给的理由是「挂观察者才连风控从未看到的信号也记下（被 `StrategyEngine` 发出却在闸门之前就没了的那种）」——这是错的。`EventEngine.dispatch` 逐 handler `catch` 之后继续下一个，所以**注册了的 handler 一定看得到每一个事件**，当前引擎里根本不存在「到不了闸门的信号」。真正的理由更窄也更具体：**规则抛异常时闸门自己一个字都不留**（异常被 dispatch 吞掉并只记一行日志，于是既没有告警也没有拦截行），而注册在它之前的 recorder 已经写下了那个信号，那是唯一能证明「当时在飞的是什么」的行。理由写错会直接产出错的测试，所以在写代码之前先把任务行改掉了。第二条理由才是结构性的：信号**就是总线事件本身**、整件事实在事件里，不像 `SignalFacts` 只存在于一次 `onEvent` 调用内部——所以这一条挂观察者什么都不损失，正好是 T313b 的镜像。**注册顺序是承重的、而且原本完全不可观测**：两行落在两张不同的表里，各自单独查回来都是对的，所以用一个记下写入次序的 `RecordStore` 装饰器（`WriteOrder`）才看得见，用例 `theSignalRowIsWrittenBeforeTheInterceptionRowThatNamesIt` 断言 `["signal:<id>", "interception:<id>"]`。顺序决定 join 的哪一半会悬空：先写信号，两次写入之间挂掉只留下「一个没被拒的信号」；先写拦截，则留下一条 `signal_event_id` 指向不存在行的拦截记录——正好是这对表存在意义所在的那个查询坏了。**靠肉眼抓到一个会空过的用例**：`aGateThatThrowsMidDecisionStillLeavesTheSignalRecorded` 在「闸门根本没被调用」时同样通过（信号有行 + 没有拦截行，可观测结果完全一样），于是给 `ThrowingRule` 加了 `consulted` 标志并**先断言它**，才证明闸门真的跑了、异常真的逃出了闸门。另外**读代码而不是靠推断**确认了 `RiskPipeline.checkSignal` 不捕获任何东西（若它把异常吞成「无意见」，sizer 会照常算出 min-notional 拒绝、留下一条拦截行，用例反而会因另一个原因失败——但这个「反而」必须查过才能说）。**读操作不需要同步装饰器**，这一点与 `OrderSenderTest` 的 `SharedOrderStore` 不同、并且写进了测试类 javadoc 免得下一个人照抄：引擎在一轮 handler 跑完之后、在 quiescence 锁内写 `completedThrough`，等待方在同一个锁内读它，所以 `awaitQuiescence` 这道屏障**同时就是** happens-before 边界（`OrderSenderTest` 需要装饰器是因为它读的是一个 worker 线程可能还在写的 store）。**journal 重放会重复落行，我刻意没有去重**：`signals` 的主键是自增 `seq`，`event_id` 是 join 键而不是身份；在这里去重意味着进程活着就得记住见过的每一个 event id，而闸门的拦截行有完全相同的问题——所以修法属于「把 journal 重放进一个活引擎」那件事（T313 已经记下过：接一个不发送的 outbox 才能重建状态而不碰交易所），不属于各个写入方。写进了类 javadoc。**变异测试 4 个：4 杀 / 0 存活 / 0 无效**，源文件按 sha256 校验还原、还原后全量 verify 仍绿。变异体：不落行、**落一个重建的事件**（关键的一个——`SignalEvent.of` 每次都取新的 `eventId`，于是每个字段都读得回来对、但 join 不上任何拦截行）、顺手把信号再 publish 一次、落两次。**第三个变异体跑了 190 秒，这本身是信息不是噪声**：一个会把信号再 publish 出去的 recorder 造成同轮内无界的级联（publish → dispatch → publish），把它变成一条失败断言而不是永久挂住的，正是引擎自己的 `DEFAULT_QUIESCENCE_TIMEOUT`（30s，6 个引擎用例各等满一次）。所以「什么都不 publish」不是风格偏好，而是「sink」与「反馈回路」的区别；直接调用的那几个用例由 `refuseToPublish`（一个被调用就 fail 的 publisher）钉住。**一个刻意没有做成变异体的点**：`BacktestRunner` 里 recorder-在-gate-之前 的注册顺序——那里没有任何测试能抓到它，也**不需要**抓：回测两行都写进没人读的 `InMemoryRecordStore`，且不存在「两次写入之间进程挂掉」这回事，所以悬空 join 在回测里不可能发生。这条约束只在 paper/live 对着 JDBC 时才承重 → 交给 **T319**，其任务行已改成点名这条顺序、并要求**断言注册顺序**而不是靠 code review 守（`StartupWiring` 目前只注册了 `StrategyEngine`，recorder/gate/OMS 接进这条序列正是 T319 的活）。**没有新增模块依赖边**：alpha-risk 本来就依赖 alpha-engine（`RiskGate implements EventHandler`），红线扫描仍绿。 |
| 2026-09-12 01:40 | **进度审计**（用户交接核查：另一 agent 完成 P2 与 P3 前半） | P0/P1/P2 已完成；P3 完成 **16/23**：T301-T314（含 T313b/T313c）☑、T315 ⏳ 进行中、T316-T320 ☐ 待办、T321 ⊘ 阻塞（需 testnet API Key）。`mvn clean verify` **BUILD SUCCESS，576 个用例 0 失败**。核查发现三点：①**T315 三个文件在磁盘但未 git 提交**（`BinanceExchangeInfoParser` 182 行 / `BinanceTradingRulesFetcher` 183 行 / 其测试 252 行，编译通过），且**未接入在线装配** —— `TradingRulesProvider` 目前只有回测用的 `FixedTradingRulesProvider`（`BacktestWiring`），在线下单的精度对齐尚未生效；②**在线链路未通电**：`StartupWiring` 只注册了 `StrategyEngine`，`SignalRecorder → RiskGate → OMS` 序列待 T319 接入，因此场景 2（模拟盘全链路）与场景 3（硬闸门端到端）**还不能验收** —— 风控与 OMS 的逻辑与测试已就绪，缺的是接线；③P2 产出物已落地：`data/*.csv` 历史数据、`reports/*.html` 回测报告在仓库根。已同步更新 plan.md（§2 阶段总览加进度列、新增 §6.0 P3 进度映射、§9 风险登记册更新、§10 下一步重排）。下一步：T315 接线 → T316 签名 REST → T317 user data stream → T318/T318b 对账与快照 → T319 通电 → T320 离线端到端。 |
